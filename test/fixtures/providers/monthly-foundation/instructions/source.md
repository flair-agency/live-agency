# Synthetic monthly activity handoff
Return the synthetic normalized activity snapshot supplied by the test driver.
Echo the requestId, capability, version and context unchanged in a done response.
Do not access a browser, device or external service.
