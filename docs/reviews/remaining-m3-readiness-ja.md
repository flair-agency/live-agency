# 招待・LIVE・コインと残存SkillのM3準備

2026-09-08。所有者が指定した30分間のローカル移行準備。実サービスの受入完了ではない。

変更カード：主分類G。招待・LIVEの移行手順の適用範囲と、招待・LIVE・コインのテスト所有境界を対象とする。業務契約、承認条件、対象・計画への束縛、読戻し、既存経路の復旧条件を維持する。完了条件は旧MCP手順の適用範囲の明記、親fixture依存の移管、所有者テストと親統合テストの成功、変更文書の配布確認。次のゲートは正式な依存版・配布版と対象を選択した導入検証。並列作業なし。公開・インストール・本番操作・pin更新は行わず、所有差分のみを戻せる状態で残す。

## 完了した変更

- 招待とLIVEのSKILL.mdおよび旧dual-run参照文書で、Creator Scouting MCP経路の手順をその経路を明示的に選ぶ場合に限定。旧経路を選んだ場合の比較・スケジュール条件は保持した。
- 招待・LIVE・コインの親fixtureを使うProvider発見テスト3件を、親の `test/remaining-skill-source-discovery.test.mjs` へ移管。既存のテスト本体とassertionを保持した。
- 招待テストのLark定数importを宣言済みパッケージの公開exportへ変更。親のProviderソース相対参照を除去した。
- LIVE・コインから移管後に不要になったファイル操作importを除去した。

子の本体実装、パッケージ版、依存指定、既存インストール、submodule pinは変更していない。親と子の既存staged/unstaged区分を保持する。

## 検証と限界

招待・LIVE・コインの所有者テスト、移した親統合テスト、独立caller/API操作チェックの合計59件が成功。さらに3つの子ソースをそれぞれ一時ディレクトリへコピーし、必要な開発依存をコピーした状態でも所有者テストが成功した。親fixture・親ソースへの参照が残らないことを検索で確認した。

この隔離検証はレジストリインストールではない。招待・LIVEが指定するLark Providerは1.0.0、コピーした開発依存は1.2.0であり、指定版での互換性を証明していない。間接依存の範囲指定と実版の一覧も機械可読記録に保持する。コインの直接依存指定とコピー実版に差はなかった。

未導入候補の先行ソース検査では9所有者の既存テストが成功した。LIVE指標圧縮は子testディレクトリを持たず、親に既存の検証がある。Baseバックアップは手順主体であり、子の実行テストがないことを成功件数へ含めない。いずれもM3完了とは扱わない。

機械可読ログはignored `tmp/v2-project/source-tests/`、`moved-discovery-tests.json`、`isolated-source-tests.json`。隔離ディレクトリと実行ログの場所は後者に記録した。実データ・資格情報・サービス識別子はこの記録に含めない。

## 評価更新の安全条件確認

追加変更カード：主分類G。`creator-insight-sync` の既存実装について、承認不足・計画後の宛先変更・更新結果不明を所有者の合成テストで確かめる。業務契約と本体実装は変更しない。完了条件は書込み回数と読戻し結果を含む直接検証。実actor問題の再現、正式名・配布・導入・実宛先は別ゲート。並列なし、テスト差分のみ取り消し可能。

## プロフィール履歴削除の不確定応答確認

追加変更カード：主分類G。プロフィール履歴圧縮の合成テストのみを対象とし、削除応答喪失時の読戻しと、保持対象の消失を成功にしない条件を検証する。実削除・バックアップ作成・復旧・本体変更は選択しない。次ゲートは保持する証拠の仕様確定と、指定版導入および明示承認された実運用検証。並列なし、所有者テスト差分のみ取り消し可能。

## バックアップ保持計画の不要依存

追加変更カード：主分類G。保持計画Skillの未使用依存 `@flair-agency/lark-base-disaster-recovery-drill` と親lockfileの対応する宣言だけを除去する。ソース・テスト・文書の検索で使用箇所がpackage.jsonのみと確認した。保持計画の入力契約、ドリル参照保護、削除権限は維持する。完了条件は所有者テスト、不要Skillを含まない隔離ソース実行、配布内容とlockfileの一致。導入・公開・バージョン選択は次ゲート。並列なし。2ファイルの所有差分を戻して復旧でき、既存インストールは変更しない。

## 検証結果の追記

- 招待・LIVEの更新アーカイブは、SKILL.md・旧dual-run文書がソースとbyte一致し、相対リンク先も同梱されている。移管した3テスト本体は移管前のSHA-256と一致した。
- 評価更新は追加4件を含む所有者9件が成功。承認不足と宛先変更は書込みゼロ、応答喪失後の読戻し成功は書込み1回、未確認の結果は再送せず停止する。
- プロフィール履歴圧縮は追加2件を含む所有者8件が成功。削除応答喪失後の読戻し成功と、保持対象消失時の失敗を区別した。実削除は行っていない。
- 保持計画は所有者7件と親依存境界3件が成功。復旧Skillを含まない隔離コピーでも7件が成功し、配布manifestと親lockfileの依存が一致した。
- LIVE指標圧縮・record ID互換・依存境界の親8件が成功。11候補アーカイブの相対文書リンク、静的importと直接依存の検査に欠落はなかった。動的解決とレジストリ取得を証明する検査ではない。

Lark Providerの1.0.0指定と開発実版1.2.0の差は、評価、招待記録、招待重複整理、LIVE記録、LIVE履歴圧縮、LIVE指標圧縮、プロフィール圧縮の計7候補にある。依存版を今回一律に書き換えず、指定版導入または正式な更新版の選択と直接互換検証を残す。

残る意味・責務の判断材料は[契約判断資料](remaining-m3-contract-decisions-ja.md)。新名称や実行経路は未採用であり、合成結果を業務受入へ繰り上げない。

保持計画は展開したローカル配布アーカイブのCLIでも、合成5組から保持3組・候補2組（4オブジェクト）を生成し、出力権限0600を確認した。入力未指定・復旧参照の完全性不足ではexit 1、出力なし。復旧Skillは同環境に存在しない。これは依存をコピーしたローカルarchive検証であり、registry installationではない。記録は `tmp/v2-project/retention-archive-cli.json`。

最終統合実行は、変更6所有者と親の移管／caller／指標／record ID／依存境界を合わせて91件成功、失敗0。変更所有者のpublic-content検査、変更文書の相対リンク、親子のdiff --checkも成功した。

依存版メタデータの読取りでは、GitHub CLIの既存認証はread:packages不足でHTTP 403だった。認証設定を変更せず既存npm設定で確認したところ、Lark Provider 1.0.0のversionとdist.integrityは取得できた。これは指定版がregistryから参照できることの確認であり、このメタデータ確認時点ではアーカイブ取得・インストール・1.0.0との互換性実行は未実施だった。GitHub CLIの失敗を全認証経路の失敗やパッケージ不存在と扱わない。メタデータは `tmp/v2-project/lark-exact-version-metadata.json` に保持した。

### 指定依存1.0.0の追跡結果

その後、ライフサイクルスクリプトを無効にして1.0.0を一時取得し、registryメタデータ・npm pack結果・実アーカイブのSHA-512 integrityが一致した。配布manifestは `@flair-agency/creator-monthly-activity-reconcile@1.0.0` を依存に含んでいた。現在のProvider→Skill依存禁止と整合しない旧配布グラフである。招待・LIVEの隔離コピーにはこの旧Skillが存在しないため、指定版でのテストは依存事前検査で止め、実行していない。元の依存版不一致を「両方で成功」と扱わない。

次の具体的な修正候補は、前述7SkillのLark依存を採用済み開発環境の1.2.0へ更新し、親lockfile・固定版archive・直接テストを同時検証すること。今回はその7宣言の更新・旧Skillの追加・既存環境変更は行っていない。正式なSkill名・配布版の採用と切り分け、Providerの逆依存を戻さない。記録は `tmp/v2-project/lark-registry-archive.json` と `lark-exact-compatibility.json`。

## 作業窓の結了

今回の作業窓は2026-09-08 14:53 JSTから約30分。変更6所有者の差分、staging未変更、検証ログ、Project読戻しを `tmp/v2-project/thirty-minute-work-receipt.json` に束ねた。Projectは完了1・進行中9・未着手15。今回の準備をM3完了へ繰り上げていない。

復旧は今回の所有差分（手順書・テスト・保持計画の依存宣言・親lockfile・記録）と対象Draftの編集に限定する。既存の別変更を含めたreset、既存インストールの除去、本番データの巻戻しは不要。ソースは未commitのレビュー可能な状態で残した。

## LIVE履歴・招待履歴の停止条件を追加検証

変更カード：主分類G、対象2Skillの合成テストのみ。既存の削除・復元コード、保持規則、承認条件は不変。応答喪失後の読戻し成功、未確定削除で停止、保持対象の欠落、承認hash不足で書込み0を検証する。完了条件は所有者テストと既に固定した一時インストールの対応コードでの実行。外部操作・新たな削除承認・公開は含めない。追加テスト差分だけを戻して復旧可能。並列なし。

追加8ケースを含む2所有者22件が成功し、既に固定済みの一時インストールの同一コードに対しても22件成功。削除応答が失われても再送0、結果未確定・保持行欠落は停止、承認hash不足は書込み0を確認した。業務コードや保持規則は不変。証拠は `hour-compaction-safety-installed-tests.json`。

継続作業の実時間は2026-09-08 15:37:12〜16:37:14 JST（約1時間）。最終Project読戻しは完了1・進行中13・未着手11、指定外の項目は保持。最終候補の55件、導入済みcompactionの22件、既存7候補の88件の各検証範囲を区別して記録した。親indexは空のまま、4所有者のソースcheckpointはclean。新規所有者のremote登録・親pin採用・正式公開・実Provider受入・稼働切替は未実施。
