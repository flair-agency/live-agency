# Lark画像と履歴書込み — 仕様根拠・接続検証記録

## 現在の結論 — 2026-09-09

公式CLI固定版ソースを実装根拠として残す方針は所有者採用済み。Aが必要とするLark能力（#30）は、既存画像保持、招待の画像付き作成・時刻更新、両Skillの混在計画まで実装・検証を完了した。過去の選択済み開発実接続を再利用し、今回追加した複数/混在経路は合成CLIと候補インストールで確認した。

| 対象 | 現在の状態 | 次のゲート |
| --- | --- | --- |
| Lark共有能力 #30 | 範囲内完了。Provider 6ecd8c9 | 個別Skill受入れは別項目で継続 |
| プロフィール #13 | ソースM2受入済み。新規画像と混在経路を検証済み | 既存合成行への画像1枚の具体的承認・実行・照合 |
| 招待 #14 / source #6 | 型付き意味、混在計画、開発create/update/imageを検証済み | BackStageの実行環境・対象の指定と現在の参照結果の検証 |
| 配布 #31 | 6候補の固定lock一時導入、128ファイル一致、42テストとinteractive source選択2テスト成功 | 最終版・依存宣言・親pin・公開対象の選択 |

Skillの説明/テスト差分は招待2b9b929、Profile82ccb40に採用。説明追記後の2archiveは33ファイルがソース一致し、各5相対リンクとhelper exportを確認済み。固定lockの実行検証は追記前の説明を持つ候補で、コードは同一。Profileの既存1.1.0依存を隠すoverrideは加えていない。

今回の追加実サービス書込みは0。プロフィール承認とBackStage対象選択を非同期で依頼中であり、未回答を承認として扱わない。公開・稼働側への導入・親pin・本番切替は実施していない。以下は当時の調査・実装記録で、古い「判断待ち」「未実装」は現在の状態を上書きしない。

## 初回調査（仕様根拠の採用前）


2026-09-09。#30の既存履歴への画像追加について、実装の前提を整理した。これは承認済み仕様ではない。

## 確認できたこと

インストール済みCLI 1.0.93と同じ公式リポジトリのcommit `2aebe8970f0a472dfc864b6ac3d19d080e75041f` を確認した。

- [画像追加処理](https://github.com/larksuite/cli/blob/2aebe8970f0a472dfc864b6ac3d19d080e75041f/shortcuts/base/record_upload_attachment.go)は、アップロードしたfile tokenを `attachments[recordId][fieldId]` に置き、Base v3の `append_attachments` へ送る。
- [共通の認証種別](https://github.com/larksuite/cli/blob/2aebe8970f0a472dfc864b6ac3d19d080e75041f/shortcuts/base/base_command_common.go)はUserとBot。画像追加shortcutは `base:record:update`、`base:field:read`、`docs:document.media:upload` の3権限を宣言する。この組合せはshortcut全体の宣言であり、末端APIの最小権限の証明ではない。
- [公式の実行テスト](https://github.com/larksuite/cli/blob/2aebe8970f0a472dfc864b6ac3d19d080e75041f/shortcuts/base/base_execute_test.go)に、追加したfile tokenを含む成功応答のfixtureがある。既存画像保持や障害時の実サービス挙動を保証するものではない。
- 公式ドキュメントの索引と追加検索からは、今回のAPIそのものの完全な仕様ページを特定できなかった。「存在しない」とは断定しない。

## 初回調査で停止した理由

共有transportの `normalizeLarkApiOperationContract` は、verifiedな操作のevidence URLを `open.larksuite.com` または `open.feishu.cn` に限定する。[Principal selection手順](../../packages/lark-transport/docs/principal-selection.md)も現在の公式endpoint文書を根拠としている。

公式GitHubの固定commitを根拠にした候補は、この条件でimport時に停止した。操作メタデータを従来のunverifiedに戻し、実装・テストの下書きは私有一時領域に移した。共通transportを緩和して通したり、架空の公式URLを付けたりしていない。

## 選択肢

1. **公式endpoint文書を引き続き必須にする。** 現行契約を保持する。API仕様の特定まで画像追加は停止。他のA検証は継続できる。
2. **公式クライアントの固定版ソースも、明示的な別種の根拠として採用する。** 共有transportのevidence契約を変更するため、独立した変更パッケージとする。公式repository、40桁commit、対象file、内容hash、確認日を必須とし、一般のGitHub URLや可変branch/tag、forkは認めない。APIの最小scopeや実接続確認済みであると誤認させない表示を保つ。Provider側の根拠レビューと、開発用の個別登録承認・既存画像保持・bytesによるreadbackは別途必要。

後者を採用しても画像追加や運用切替の承認にはならない。前者のままでも、今回の招待履歴作成と日時更新計画は影響を受けない。

## 保持した成果物と復帰点

私有 `tmp/v2-project/invitation-hour-cli-*` に取得元・版・ソースを保持。`selected-image-appender-draft.js` と対応テストは未接続の下書きであり、成功テストや実装済み能力として数えない。再開時は採用された根拠条件を満たすことを先に確認し、下書きを再レビューしてから所有者ソースへ戻す。Lark Providerはこの調査前の `7d52a25` を維持する。

## Skill依存経路の確認 — 所有者からの追加確認

承認依頼は保留。主分類G、コードと既存テストの読取り確認。実装・運用・依存リンクの変更なし。

| Skill | 新規の画像付き履歴 | 既存の画像未添付履歴 |
| --- | --- | --- |
| プロフィール | upload後、file tokenを含めてcreateする。appendは呼ばない | 同じ内容で対象時刻条件を満たす画像なし候補が1件ならappendを計画する。候補複数は競合 |
| 招待可否 | 現行applyRefreshは先に履歴create、その後uploadとappendを呼ぶ。通常経路でも必要 | 同一観測時刻・同一内容の画像なし履歴が1件ならappendを計画する |

根拠はプロフィール `scripts/profile_lark_runtime.mjs` のapplyProfilePlanと `scripts/profile_sync_core.mjs` のattachmentCandidates、招待 `scripts/invitation_lark_runtime.mjs` のapplyRefreshと `scripts/invitation_state_core.mjs` のexactTime分岐。既存の直接回帰2ファイルで23テスト成功（私有 `append-dependency-review-tests.log`）。本番履歴の存在確認や手動復旧の検証はしていない。

訂正：画像追加を両Skill共通の「失敗時の復旧だけ」と説明したのは誤り。招待では通常の画像付き作成にも使う。画像付き新規登録の実接続検証済みという実績はプロフィールに限る。招待の選択済みv2 payload builderは画像を含む計画を明示的に拒否し、画像なし作成・日時更新だけを検証している。

現状のまま画像を扱う両Skillの全機能受入れにはappend対応が残る。ただしプロフィール新規作成の経路自体は依存しない。招待をプロフィール同様のupload→画像込みcreateへ変更する方法は別の実装・検証であり、既に対応済みとは扱えない。手動復旧を安全な代替として確認した証拠もない。

推奨：#30の完了を一律条件と説明せず、(1)検証済みの作成/日時更新/画像読取、(2)招待の通常画像付き登録、(3)既存の未添付画像追加、に受入れ条件を分けて記録する。画像機能の除外や手動代替による切替条件変更は今回決定していない。公式CLIソースを根拠として採用するかは、この必要性整理とは別問題であり、保留を維持する。

## 採用・実装開始

所有者が、公式ソースの実装根拠を残し、関連不具合時の原因候補として参照できる方針を承認した。主分類E、副分類A/B。共有transportの根拠形式とProviderの画像追加候補を対象とする。公式repository固定commit・file・content hash・確認日を選択bindingへ保持し、根拠変更で旧bindingを無効化する。既存の公式文書形式は互換維持。未知のrepository、可変branch、hash欠落を拒否する。実接続や画像登録は別の具体的計画と承認を必要とする。ローカルテスト・配布閉包確認までを本パッケージとし、公開・親pin採用・運用切替を含めない。並列agentなし。

## 承認方針の実装結果

共有transport `48dd77e` は公式CLI固定commit/file/content hashを別種の根拠として保持し、根拠変更で選択bindingを無効化する。既存文書型との互換を維持。Provider `0fab06a` は1画像のupload→append、既存画像と他fieldの保持確認、byte照合、durable eventへの根拠保存、再送禁止を実装。既存画像0/1、User/Bot、変更・応答消失・記録失敗を直接検証した。

共有transport83、Provider178、関連結合18テスト成功。配布候補2件の55ファイルがソースと一致し、隔離候補で38テストと新export2件を確認。既存の開発依存を再利用した検証で、新規固定lockインストールや公開ではない。最初のアーカイブ検査は検査用配置にCLI依存がなく停止し、既存CLIを正しく参照して解消した。新規依存のインストールなし。親の独立inventoryは新しい根拠型を個別確認する形に更新し、他の既存差分は保持した。

開発用画像追加の計画 `83417eeb8f8d381d60652381f01508a32655f7359b4ba312cba3aa5bd90f5b13` を準備。以前作った画像付き架空プロフィール1件の既存画像1点を保持し、別の68-byte PNGを1点追加するProvider検証。新規履歴0・削除0、画像upload1・append1。実接続の読取りで現状と既存画像hashを確認済み。実画像追加は未承認・未実行。私有 `append-development-{plan,intent,selections,baseline}.json` と実行準備スクリプトを保持。PNGのCRC・展開データも検査済み。

承認後は画像保持を実検証し、その結果を基にSkill側の画像経路の配線へ進む。今回のProvider実装だけで招待Skillの画像付き登録が完了したとは扱わない。故障時は保存eventのoperationEvidenceと固定版ソースを原因候補として参照し、権限・field/record差分・画像内容・応答不明と区別する。公開、親pin、運用切替なし。

## 承認済み開発画像追加の結果

所有者の「はい」で上記計画・intentを承認として記録し、1回だけ実行した。既存画像1点を保持してテスト画像1点を追加し、合計2点のbyte hashとその他の項目・field定義の保持を再読取で確認。新規履歴0・削除0。私有 append-development-approval.json、attempt、event-0〜4、result.jsonを保存。途中再送なし。実装根拠は各eventに残り、関連不具合の調査で参照できる。

Providerの実接続検証は成功。Skill画像経路の統合・受入れは残るため#30は閉じずTodoへ戻す。利用枠を消費済みとの連絡を受け、今回の実行・確認・記録で区切り、追加実装や広範なテストは開始しない。画像の自動削除による巻戻しは行わない。復旧が必要なら保存baseline/resultで対象を特定し、既存画像を保護した別の変更計画を用意する。公開・親pin採用・運用切替なし。

## A限定の再開パッケージ

主分類D、副分類B。境界はプロフィールSkillと選択済みProviderのローカル結合。既存の画像未添付履歴1件に対する正確な計画・件数承認、再読取、応答不明時の再送禁止を保持する。最初の完了条件はSkill入口からselected CLIまでの合成結合検証（成功・応答消失・承認不足・古い計画）。既存実装で契約を満たすなら不要なソース変更はしない。次は招待画像経路の統合、続いて両Skill受入れ・A配布確認・個別切替の順。外部登録・公開・切替には各具体的な選択と承認が必要。B–Dは着手しない。新テストを除けば今回の変更を戻せる。並列agentなし。モデル設定変更なし。

### プロフィール既存未添付経路の結合結果

既存Skill入口を選択済みAppenderへ注入し、画像未添付の完全一致履歴1件への追加を検証した。新規レコード0、画像追加1、その他fieldの保持、byte読取、応答消失時の照合と再送禁止、承認件数不足・画像改変・古い計画の書込ゼロを確認。新しい親結合テスト3件と既存画像付きcreate・inventoryを合わせ8件成功。対象差分確認済み。変更所有者は親のtest/selected-profile-image-append-integration.test.mjsとこの記録・statusのみ。Skill/Providerの製品コードは変更不要、既存の未採用差分を保持。実接続・新たな承認消費なし。次は招待の新規create後のIDを元の承認計画へ束縛する画像結合。A全体の受入れ・配布・切替は未完了。

## 招待画像付き作成の実装パッケージ

主分類D、副分類B。境界はLark Providerの1件create→upload→append合成と招待Skillの純粋なpayload準備。既存の業務順序・型付きversion 3計画・画像と件数の承認を維持する。新規レコードIDは同じcreate応答と再読取から束縛し、画像追加用の派生intentと親intentをdurable eventで結ぶ。作成前に画像・全選択・fieldを確認し、応答不明は後続処理を止める。完了条件は公開export、知識、直接拒否検証と型付きSkill結合が通り、所有者の差分を保存したcheckpoint。既存レコードへの再開は別の既存Appenderを用いる。次の実接続は新しい具体的計画を提示してから。今回の変更のみ戻せる。新言語・依存導入なし、並列agentなし。

### 招待画像付き作成の結果と残条件

Provider e588027、招待Skill b9f3889をローカルに保存。画像を持つ新規1件のcreate→upload→appendを親intentへ束縛し、作成後のID・内容を再読取してから画像処理へ進める。既存未添付の再開も型付きSkill入口から検証済み。応答消失・対象/画像/field変更・権限失効・event失敗は後続の書込や再送を止める。直接Provider185、Skill22、A結合26件成功。配布候補3件の74ファイルが一致し、隔離候補52件成功。保存済み実開発データから候補で同じ計画・intentも再現できた。5つのA候補の静的配布検査は108ファイル・41export・82相対import・31相対Markdownリンクで欠落0。

開発用画像付き招待履歴1件の計画 35fcdbe258c48c14cb4ac1809eef8268984e405933bf1deb3620a7c8e5612343、intent d8250f99dffd499cd47d3d38a1cf387492974f0056e5c6ed5bea31a86bbe4221 を提示し承認待ち。新規1・画像1・時刻更新0・削除0、既存2件保持。私有 invitation-image-development-* に具体的計画・対象・実行準備を保存。初回準備は補助スクリプトの関数名誤りで計画保存後に停止し、保存済み計画を再利用して読取りのみで完了した。登録は未実行。

既存スキル検証器はPyYAML不足で起動できず、依存は導入しない。今回はfrontmatter変更なしで、実行コード・配布物・参照の確認は成功。製品ソース変更・テスト結果とこの検証器の制限を区別する。親pin、公開、運用切替なし。

Aの残条件：この新規1件の実接続、プロフィール既存未添付の実接続準備、画像付き複数件・作成/時刻更新/未添付再開の混在計画を扱う合成、明示選択したソース取得からの利用経路受入れ、最終配布版・運用切替。現在の1件合成を通常の複数対象運用全体の完了とは扱わない。復帰点は上記2子commitと私有計画。

### プロフィール未添付の開発計画・配布確認

既存の架空プロフィール1件（followers 100）について、新規0・画像追加1・削除0の正確な再開計画を準備した。plan fc38e7dd1ec8fd4e999cb5e283ddda19d832dde2d667e09412ee6e2acede8590、intent 90da27ce1c0d051818fca1154b1d53561065ac40c30de8bae7c784d4f67adac5。別の画像付き履歴を含む現在の開発履歴と画像を読取り確認。承認・実行は未実施。私有 profile-resume-development-* に計画・対象・専用実行手順を保存し、承認なしでは起動しない。配布候補から保存済み履歴と検証済みhashで計画・intentが完全再現した（profile-resume-archive-result.json）。この再現は新たな実画像読取ではない。

既存の未採用Skill差分は、承認済みの招待可否型・MCP手順の適用範囲・Provider依存整理と関連検証だった。今回のcommitには混ぜていない。移動された招待source discovery確認は親側に存在し、型付きhandoffと合わせて再確認した。最終配布採用ではこれら既存差分も明示的に採用する必要がある。次のモデル選択は開発方針のAstra/lowを維持し、現在の設定変更を行ったとは扱わない。

## 承認済み招待画像付き登録の実接続結果

所有者の「この1件の登録を承認」に基づき、計画35fcdbe258c48c14cb4ac1809eef8268984e405933bf1deb3620a7c8e5612343を1回実行。選択済み開発App/Botで新規履歴1・画像1が成功。画像byte照合、既存2件の完全保持、再計画でcreate/update/attach各0・alreadyApplied1を確認した。私有 invitation-image-development-{approval,attempt,event-0〜7,result,readback,replay}.jsonを保持。再送・削除・本番変更なし。今回の承認は消費済みで再利用しない。復旧が必要な場合は保存した対象IDと画像token・baselineから別の計画を用意し、無条件に再送しない。プロフィール未添付計画は未承認・未実行。複数件/混在計画と利用経路受入れは残り、移行全体の完了ではない。

## 複数画像付き新規作成のパッケージ

主分類D、副分類B。Providerの1バッチ作成と招待Skillの純粋なpayload準備を対象に、既存の1件intentを維持して複数件候補を追加する。上限100件、画像なし行も同じbatchで保持し、全create値・画像・選択を親intentへ束縛する。全画像と下流権限を作成前に確認し、返却された全IDと内容を照合してから順に添付。応答不明・対応不一致・途中失敗で残りの画像処理を停止する。完了条件は複数件の直接/Skill結合検証と配布参照確認。実接続・公開・pin採用なし。次は同一計画の時刻更新と既存未添付への明示的合成。既存の実接続済み1件経路を復帰点に維持。並列agentなし。

### 複数件バッチの結果

Provider c32c7b5、招待Skill f7b1516をローカルに保存。画像なし行を含む1–100件の新規作成を1回のbatchに保ち、全返却ID・内容を確認して画像あり行へ順に添付する。Bot/Userの直接検証、3履歴/異なる2画像の型付きSkill結合、応答消失後の残件特定、画像順序・内容・権限・event異常の停止を確認。Provider191、Skill22、関連結合29件成功。配布候補3件の76ファイルが一致し、隔離候補61件成功（既存依存を再利用、新規固定lock導入ではない）。差分確認済み。既存の1件intentと実接続証拠は維持。今回の外部登録0、承認消費0、公開/pin/運用変更なし。復帰点は直前のe588027/b9f3889。親の新しいバッチ結合テストと共通合成fixtureは未採用のローカル差分として保持。

次のパッケージは招待の同一計画内の時刻更新・新規作成・既存画像再開の明示的合成。Profileの新規画像はupload-before-createを保持するため、このpost-create batchを流用しない。Profile複数画像付きcreateの合成も残る。今回の関数は混在計画を拒否し、副作用を省略して実行しない。

## 1時間のA実装継続 — 招待混在計画

主分類D、副分類B。開始2026-09-09 02:52 UTC、目安03:52 UTC。境界はProviderの承認済み子intent合成と招待Skillの純粋payload準備。時刻更新→新規batch→既存画像再開→新規画像の既存順序を維持し、全子intentを同じplan/actor/resourceへ束縛。最初の変更前に全field/baseline/画像/下流preflightを確認し、途中の未確認結果で後続を止める。完了条件は混在正常系、部分成功と再計画、宛先/順序/権限変更拒否の結合確認と配布検証。次はProfileのupload-before-createを保持した複数画像合成。既存の個別adapterを復帰点として保存。新たな実接続には個別承認を適用。並列agentなし。

### 2026-09-09 招待の混在書込み 接続完了（1時間作業の第1区切り）

- Provider `3fab011`、招待Skill `a12772b`。時刻更新・新規登録・既存画像補完・新規画像添付を、同一計画・同一actor・同一宛先の親intentで順序付けた。各段階の再読取と永続イベントが確認できない場合は後続を停止する。
- 書込み前に全段階の権限・フィールド・既存行・既存画像のバイト・ローカル画像を確認する。既存の単体経路と入力・副作用順を保持し、100件境界を超える計画を暗黙分割しない。
- 実検証: Provider 197、招待Skill 22、選択CLI接続とcall-site inventory 31テストが成功。未公開3候補アーカイブ78ファイルのバイト一致と隔離実行99テストも成功。証跡: `tmp/v2-project/mixed-*-tests.log`、`mixed-history-archive-result.json`。
- 外部書込み・公開・親pin採用・本番切替は実施していない。以前からあるSkillドキュメント等の未確定変更はコミット対象外。復帰は従来の単体writerと前チェックポイントで可能。
- 次の区切り: プロフィールの複数画像付き新規登録。既存の「全画像upload→batchCreate」の順序と、曖昧応答の読取照合を保持する。

### 2026-09-09 プロフィール複数画像 接続変更カード

- 主分類D、副分類B。境界: Providerの選択済み書込みと既存Profile Skillのクライアント接続。
- 不変条件: 同一計画/actor/宛先、全画像の事前確認、画像と行の対応、upload先行、1回のcreate、曖昧書込みの再送禁止。新規の業務判断や本番選択は行わない。
- 完了条件: 画像あり/なし混在バッチ、順序・件数・権限・画像変更・失敗時の残存状態を実装とテストで確認し、候補アーカイブから再現する。
- 次ゲート: 実接続は具体的な計画への承認、公開/pin/切替は別途。既存単体writerを復帰路として保持。
- 分担なし。新規スクリプトは既存Node >=22互換のJavaScript。

### 2026-09-09 プロフィール複数画像 新規登録接続完了

- Providerの `selected-image-batch-creator` を追加。画像あり2件・なし1件を、全画像upload→1回のbatchCreateの順序で登録し、行と画像tokenの対応を再読取で確認する。画像バイトと業務計画全体はProfile Skillが照合する。
- 検証: Provider 205、Profile Skill 19、対象接続/inventory 17テスト成功。未公開3アーカイブ79ファイルのバイト一致、隔離52テスト成功。証跡 `profile-image-batch-*-tests.log`、`profile-image-batch-archive-result.json`。
- 部分登録時は残件を再送しない。完全登録後の応答喪失は既存Skillの読取照合で解決できる。以前の単体writerを保持し、外部書込み/公開/親pin採用はなし。

### 2026-09-09 プロフィール混在計画 変更カード

- 主分類D、副分類B。境界: 既存Profile業務計画とProviderの選択済みwriter接続。
- 不変条件: 既存画像補完を先に処理し、新規画像全件upload後にbatchCreateする。全段階の事前検証、同一計画/actor/宛先、失敗時後続停止を維持。
- 完了条件: 画像補完と新規登録が混在する既存の計画を副作用の省略なく実行し、選択CLIの合成テストと候補アーカイブで同一結果/停止を確認する。
- 次ゲート: 外部書込みは具体的な計画への承認、公開と本番切替は別。単体writerと第2区切りを復帰路として保持。分担なし。

### 2026-09-09 プロフィール混在接続 完了

- Provider `6ecd8c9`、Profile Skill `fa76f68`。既存画像補完→新規画像全件upload→batchCreateの順序を1つのレビュー済み親intentに束縛した。単独処理と画像なし行も副作用を追加せず通る。
- Provider 209、Profile Skill 19、関連接続/inventory 20テスト成功。未公開3アーカイブ81ファイルが一致し、隔離75テストも成功。証跡: `profile-mixed-*-tests.log`、`profile-mixed-archive-result.json`。
- 既存画像の応答喪失で新規処理を停止し、完全な最終createの応答喪失はSkillの再読取で解決する。Skillの副作用順序・承認件数・従来単体writerを保持。外部変更/公開/pin採用なし。
- 次の検証: 既存ソースNormalizerから両混在workflowまでの正規化値/画像metadata/結果照合と、5パッケージの配布参照閉包。実観測の取得や書込みを意味しない。

### 作業枠の終了記録 — 2026-09-09

02:52 UTCから約55分、Aのみを継続した。共有能力#30を完了、#13を具体的な画像1枚の承認待ち、#6を実行環境/対象指定待ち、#14を#6待ち、#31を準備済みの後続Todoへ整理し、本文とProject状態を再読取で確認した。未回答を承認に置き換えず、この区切りで停止する。

Provider/招待/Profileの3所有者は記録したHEADでclean。親の元indexは空のまま保持し、既存の無関係な変更は維持した。親の新規混在接続テスト3ファイルと本レビュー/配布記録は親pin採用ゲートに残す。最新検証コードと提案版のnon-manifest 122ファイルは一致し、独立call-site inventoryも成功した。最終機械記録は `a-hour-final-checkpoint.json`。

公開・親pin・稼働側登録・新規の実サービス書込みは0。復帰路は従来の単体writer、各子checkpoint、および一時consumerの元lockであり、実データの巻戻しを実施したとは扱わない。次に必要な所有者入力は提示済みのProfile承認とBackStageの環境/対象指定の2点。
