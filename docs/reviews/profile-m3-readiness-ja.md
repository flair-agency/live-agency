# プロフィール記録M3：配布済み経路の準備確認

## 所有者受入れ完了 — 2026-09-09

#13の提示済み開発検証範囲を所有者が明示承認。履歴・画像登録、既存行への画像1枚補完とbyte再読取、他行・他項目保持、追加0件の再計画、対象変化・応答不明の合成検証を受入れ済みとする。Issue/ProjectはDone。以前の画像登録承認は消費済みで、新しい登録は行わない。配布候補の正式版検証・公開は#31、本番切替は#32。以下の承認待ち記述は履歴。

変更分類G：承認記録と状態の更新のみ。新規テスト・サービス操作なし。記録差分とリンクを確認し、修正が必要な場合は記録訂正とIssue再開で対応する。

## 画像補完の承認・実接続検証完了 — 2026-09-09

所有者の「この画像1枚の追加を承認」を受け、提示済み計画 fc38e7dd1ec8fd4e999cb5e283ddda19d832dde2d667e09412ee6e2acede8590 を選択済み開発環境で一度だけ実行した。新規行0・画像追加1・削除0。画像内容と対象行の非画像フィールド、他2行の保持を検証し、再計画は新規0・画像追加0だった。承認は消費済みで再実行しない。

証跡は tmp/v2-project/profile-resume-development- の approval、attempt、event-0〜4、result、readback、replay 各JSON。コード変更・公開・本番切替はなし。今後の不整合は証跡と再読取から調査し、この承認で再送や削除はしない。以下の承認待ちは実行前の履歴である。


## 現在の受入れ待ち — 2026-09-09

Source M2 #4は受入済み。共有Lark能力#30も範囲内完了。Profile 82ccb40 / Lark Provider 6ecd8c9の候補で、画像あり/なし複数登録、既存画像の補完との混在、部分/曖昧応答、画像バイトの再読取、再送防止を確認した。固定lockの一時consumerで実行し、ソースの説明とテストも採用済み。

残る具体的な確認は、既存の合成プロフィール1行へのテスト画像1枚の追加である。作成0・画像追加1・削除0、他2行を保持する計画を提示済みで、承認待ち。提案する新しい配布候補からも保存済み計画/intentが同じhashで再現できたが、実行もfreshな実サービス読取もこの作業枠では行っていない。

承認後は選択済み開発App/Baseで即時再検証し、一度だけ実行して、元の非画像フィールド/他行/画像bytesと追加0件の再計画を確認する。実スキャンの再取得や本番切替はこの承認に含まれない。正式版・依存・公開と切替は#31/#32。詳細は[現在の共有能力記録](lark-append-evidence-source-ja.md)と[配布準備](v2-release-readiness-ja.md)。以下の旧状態は当時の記録として保持する。


2026-09-08。受け入れ準備の記録・手順整理案。本番切替の承認やM3全体の完了を意味しない。

## 今回の範囲と結果

変更カード：主分類D、副分類G。対象は配布済みプロフィール記録Skillのローカル検証と移行記録。実装・パッケージ・本番設定を変更せず、既存の履歴追加限定、計画承認、画像検証、readback条件を維持する。完了条件は旧手順の適用範囲とテスト先要件の特定、通信しない検証結果の記録。並列作業なし。次のゲートは手順整理と実サービス側の明示的なテスト対象選択。今回の一時検証ファイルと文書差分だけで取り消せる。

配布済み `@flair-agency/creator-profile-record@1.1.0` を直接読み込む合成テスト12件が成功した。既存9件の検証内容は変えず、テストのimport先だけを配布物に向けた。追加3件は以下を確認した。

- 比較のみでは変更0回、承認ハッシュ未指定のapplyは変更前に拒否。
- 計画作成後に対象が消えた場合はblockedとなり、変更0回。
- 作成後に応答だけ失われた場合はreadbackで成功を確認し、作成呼出しは1回。

既存テストでは画像を新規行と同時登録する順序、画像欠落の再開、再実行時の重複防止、フィールドID、観測値と記録先IDの検証も確認した。最初は一時テストの動的importが解決できず1件失敗したが、配布済みProviderへの参照を修正後は12件すべて成功した。製品修正ではない。

証跡は `/private/tmp/live-agency-m3-profile/` のテスト、provenance、結果ログ。合成のインメモリ宛先を使用し、Larkへの通信、画像アップロード、実レコード作成は行っていない。これは製品コードを読む既存回帰テストを含むため、独立した利用者操作によるM3受け入れや実Provider検証の代わりにはしない。

## 旧MCP手順の適用範囲

配布済みSKILL.mdと `references/v2-dual-run.md` は旧Creator Scouting MCP経路と2回の定期実行を前提としている。一方、[採用済み移行計画](../migration/v2-plan.md#stage-3-m3-skill-acceptance-and-production-cutover)では選択したCLI／最小アダプターで業務を検証し、MCP全体の実装を前提にしない。

今回の確認で、計画・承認・apply・readbackはMCPを起動せず配布済み関数から実行できた。旧MCP比較はその旧経路の切替を選択したときの条件として保持し、一般のv2移行に必須と読める手順は整理する案とする。旧版との比較が有効なら再利用し、無効・未選択の旧経路を比較のためだけに稼働させない。公開手順の変更、リリース、導入は未実施。

## 実サービス検証に必要なもの

- 明示的に選択した開発用Base、実行主体、Creator／ProfileテーブルとフィールドID、対象ビュー。
- 現行読取りから生成した安定したCreatorレコードID付きmanifest。取得済みのsource-only M2 manifestに架空の宛先IDを足して本番計画に使わない。
- 検証対象の履歴追加・画像添付権限、既存状態、計画ハッシュ・件数、readbackおよび復旧条件。

次の作業は、所有者のソースで旧手順の適用範囲を整理して配布物を検証すること。実宛先の選択がない間は合成テスト先で準備し、本番既定設定・認証情報を推測して使わない。Provider間の画像取得知識の共通化は移行後のIssueに残す。

## 離席中の開発作業（2026-09-08）

オーナーから追加許可の不要な作業を継続する指示を受け、前節の手順整理を開発ソースで実施した。主分類B、副分類G。対象はプロフィールSkillのSKILL.md、旧MCP比較リファレンス、回帰テスト。旧MCP経路を明示選択した場合の比較・定期実行条件を維持し、他の経路にその実装を要求しないよう適用範囲を明記した。公開・導入・本番切替は含めない。

一時検証で追加した3件を所有者の回帰テストへ取り込み、既存の旧MCP比較7件を含む19件すべて成功した。ローカルnpmアーカイブには変更した手順がソースと同じ内容で含まれ、相対リンクも解決した。初回のpackはユーザーキャッシュ書込み制限で失敗したため、一時キャッシュを使って成功した。権限設定や既存キャッシュは変更していない。

このアーカイブは未公開の開発候補で、package.jsonの版は変更していない。同じ1.1.0の公開物を置換したり、候補を公開済み版として扱ったりしない。インストール済み1.1.0にはまだ旧手順が残る。次は公開版の選定・リリースと導入確認、独立した利用者経路での受け入れ、実宛先の選択が残る。実サービス検証の条件は前節のとおり。

所有者リポジトリは開始時にcleanで、今回の3ファイルだけが未コミット・未ステージの変更。親の既存差分とindex、子pinは保持した。対象diffと行末空白を確認した。復旧はこの3ファイルの今回差分と一時アーカイブを対象とし、インストール済み版や本番データへの復旧操作は不要。
