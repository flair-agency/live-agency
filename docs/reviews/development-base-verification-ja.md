# 開発Baseと移行候補の検証記録 — 2026-09-08

## 今回の範囲

ユーザーが指定した1時間の作業枠は18:35:06〜19:35:06 JST。
開発専用Appと通常開発・復旧検証用Baseを使う準備、読み取りProvider、
承認済みの保持計画名と招待可否の境界、ギフト履歴の照合処理を進めた。
TikTok LIVEの検証環境は実施時にユーザーが指定する。本番切替は対象外。

変更分類と完了条件は[移行状況](../migration/status.md)に記録した。
App、Base、表、フィールド、レコードの識別子と実行結果は、Git管理外の
`tmp/v2-project/` に保持する。ここには実サービスの識別子を転載しない。

## 開発環境で確認できたこと

開発用Baseと復旧用Baseの作成結果をBotで再取得し、所有者ユーザーでも
アクセスできることを確認した。通常開発BaseにはCreator表とProfile履歴表、
合成アカウント2件を作成した。Profile表の単一値・双方向リンク、日時、数値、
テキスト、添付フィールドを既存Skillのフィールド解決処理で検証した。

CLIの表作成ショートカットで作られる初期リンクは単方向・複数値だった。
Profile契約を緩めず、公式フィールド作成APIで単一値・双方向リンクを追加し、
再取得したスキーマを検証した。最初の不適合リンクは負例として保持している。
実際のクリエイター情報、Profile履歴、添付ファイルは登録していない。

ネイティブCLIのrecord-getで合成レコードIDとアカウント値は一致した。
一方、Providerで使うrecord-listは別のAPIであり、同じ成功とは扱わない。

## 残るApp権限

実行時に次の2種類の `app_scope_not_applied` を確認した。

| Appに必要な権限 | 用途 | 現在の扱い |
| --- | --- | --- |
| `tenant:tenant:readonly` | 選択したBotの所属テナントを再検証する事前確認 | ユーザー対応待ち |
| `base:record:retrieve` | Providerが使用するレコード一覧API | ユーザー対応待ち |

User Tokenの再ログイン不足と同一視しない。実際の成功を確認するまでは、
Endpointの権限表を推測で書き換えない。別Appやユーザー権限への自動切替も行わない。
権限反映後は同じ選択済み構成を再実行し、成功・停止理由を記録する。
作成済みBaseやレコードを再作成する必要はない。
19:26 JSTの同じ構成による再確認でも、組織確認の段階で
`API_APP_SCOPE_NOT_APPLIED` により停止した。この再確認はレコード一覧まで到達していない。

## ローカル実装と検証

| 所有者 | 今回の成果 | 実施した検証 |
| --- | --- | --- |
| Lark transport | App側の未適用権限を `API_APP_SCOPE_NOT_APPLIED` として識別 | 所有者テスト81件成功 |
| Lark Base Provider | Base・表・ビュー・操作を明示した読み取り専用クライアント。全ページ取得が完了しなければ停止 | 所有者テスト114件成功。うち新しい読み取り経路13件 |
| 親リポジトリ | ProviderとProfile Skillの対象抽出・添付なし計画を接続 | 統合4件成功。型不適合、不完全ページ、対象の同一性変更を検証 |
| ギフト履歴Skill | 承認計画・更新前履歴・更新後履歴・管理対象集計・同期ログを照合する純粋関数 | 所有者テスト27件成功。新規追加、欠落、重複、集計ずれ等18件を追加 |
| 保持計画Skill / maintenance | `live-agency-data-backup-retention-plan` にローカル名称と呼び出し先を統一 | 保持・復旧・maintenanceの組み合わせ19件成功 |
| 招待可否Skill | 新しい入力を招待可否に限定。送信進捗、取得不能、旧履歴の意味を自動変換しない指示を明文化 | 所有者テスト16件成功。型付きの新契約実装や実履歴の移行は未実施 |
| 呼び出し箇所の調査 | リポジトリ相対パスのSkill呼び出しが未分類になる不具合を修正 | 独立チェック2件成功、85件を分類、未分類0件 |

ギフトの純粋関数は、入力が実際に選択済み宛先から新しく完全に読み取られたことを
証明しない。その証拠は宛先側の担当が保持する。承認済みの追加ゼロ行など、
イベント由来ではない補助行は別の宛先マッピングと保全検証が必要。
全ピボットとLark派生投影は今回の受入対象へ追加していない。

読み取りProviderは書き込み・画像取得・添付ハッシュ計算を公開しない。
添付を含むProfile処理と実書き込みの受入は、これだけでは完了しない。
各Skillの実呼び出し、選択済み経路への接続状況、添付appendの未検証状態は
[Lark操作の残作業表](../migration/lark-history-operation-gaps.md)に整理した。
既存Skillの標準CLI入口は環境由来の旧クライアントを作るため、新exportの追加だけで
入口の移行が済んだとは扱わない。

## 配布状態での確認

Lark・保持計画・maintenance側はローカル8アーカイブを隔離した利用側へ入れ、
固定ロックで再構築した。ソースと配布ファイル66件の一致、関連テスト45件の成功を確認した。
ギフト側は5アーカイブと既存の固定依存バージョンを別環境へ入れ、同様に再構築した。
配布ファイル38件の一致、27件のテスト、新しい公開exportからの読み込みを確認した。
ギフトの既存Lark Provider依存は1.1.0のままであり、依存抽出の保守課題は今回変更していない。

どちらもインストールスクリプトを無効化し、既存の運用インストールを変更していない。
これはローカル候補の配布内容確認であり、レジストリ公開済み版の受入や本番切替ではない。
変更したSkillの公開内容チェックは成功した。
標準Skillメタデータ検証器は環境にPyYAMLがなく起動できなかったため、成功とは記録しない。
依存パッケージを追加インストールしてこの制約を解消する操作は行っていない。

## ローカルチェックポイント

| 所有者 | 記録したコミット |
| --- | --- |
| `packages/lark-transport` | `1af6b4f345953e9bb136a4154e08c29f6f862ac2` |
| `providers/lark-base` | `7903b107410af0982b4c8a5b5ecf6e6b32f748d6` |
| `skills/live-agency-gift-history-merge` | `8d797472a55cecd9c2d3e77254f60f03d943ad6a` |
| `skills/lark-base-maintenance` | `aa5be9fbed733bf9440033e0f20da0bcf5375292` |

親のsubmodule採用pin、公開版、運用設定は変更していない。
保持計画と招待可否のローカル候補、および親の資料・検証変更は未コミット。
既存の別作業の変更とステージ状態を保全した。

## 再開条件と復旧

App権限が反映されたら、作成済み開発Baseに対して限定読み取りを再実行する。
その後、必要な添付・追加書き込みのProvider対応と合成データの受入へ進む。
次の合成Profile計画を準備する私有ヘルパーも用意した。直近5分以内の成功した
限定読み取り証拠と、指定した合成Creator2件の一致がなければ停止する。
今回はその停止条件まで確認した。実Profile計画の成功や書き込みとは扱わず、
ヘルパーにもapply処理を含めていない。
ギフトは新規イベントを含む選択済み宛先での受入、保持計画は選択済み保存先の
完全なバックアップ一覧による計画検証が残る。

今回のローカル変更は記録した所有者コミット単位で戻せる。開発Baseと合成データは
証拠と一緒に残し、無断で削除しない。本番データの復旧は必要ない。

## 権限反映後の追記

ユーザーの設定完了後、同じBotで組織確認・フィールド取得・合成Creator2件の取得が成功した。空の履歴表はitemsを省略し、has_more=false / total=0を返すため、その明示的な空応答だけを受け付ける修正を追加した。不明・非ゼロ件数・継続ページ・null項目は引き続き停止する。関連22件のテストが成功した。

再実行した合成Profile計画は追加2件、添付0件、競合0件、対象問題0件。書き込みは未実施。新たなユーザー権限設定は不要で、次は選択済みbatch-create対応と具体的な計画承認・読戻しが必要。今回の応答修正は前の4コミットとは別の未コミット候補で、配布アーカイブの再検証は未実施。

## 追加の1時間枠 — 19:51:28 JST開始

今回は、選択済みの開発App権限が反映された状態から再開した。
前節までの権限待ち・配布未検証は過去の状態であり、現在の追加設定要求ではない。

### 登録の準備と承認対象

Lark Base Providerに、表・フィールド・値・件数・計画hashを束縛した
1回限りの登録クライアントを追加した。1バッチ1〜100件、画像なしに限定する。
Profile Skillは既存の項目組立てを純粋関数として公開し、親の連携処理が
業務上の項目対応を重複実装しない形にした。

実Appで最新のフィールドと2件の合成Creatorを読み、Profileの追加2件・添付0件・
競合0件の計画を作成した。合成値はフォロワー数100と200であり、実在アカウントの
観測ではない。計画hashを示した具体的な登録承認を、このタスクで提示済み。
現在は回答待ちであり、外部へのProfile登録は実行していない。
承認後は直前照合、登録、全項目の読戻し、同じ入力で追加0件になる再計画を行う。

応答喪失・重複した返却IDでは読戻しが全件を説明できる場合だけ成功とする。
一部だけ登録された場合は再送せず停止し、古い全件計画の再実行も防ぐ。
承認hash・件数の相違、対象アカウント・フィールドの変更も登録前に停止する。

### 添付読取りと配布参照の修正

バックアップ側の選択済み添付読取りにも、空の記録応答でitemsが省略されると
停止する不具合があった。失敗2件を再現して修正し、関連23件が成功した。
空と明示された記録一覧だけを受け付け、未知・null・継続ページの拒否、件数整合、
取得量制限、参照元変更の検知を保持した。空表から記録や添付を作り出さない。
実添付の取得・アップロード・追記は行っていない。

公式資料と現行CLIの調査はProviderの
[添付対応表](../../providers/lark-base/knowledge/history-attachment-coverage.md)に記録した。
新しい行に画像を含める手順と、既存行への追記は別の受入対象である。
`attachments:append` は引き続き未検証で、自動的な代替経路は追加していない。

独立した配布物リンク検査で、添付手順が参照する合成サンプル2ファイルの同梱漏れと、
配布外の資料への相対リンク2件を検出した。必要なサンプルだけを同梱し、資料は
実在を確認したソースURLへ変更した。相対文書参照と静的importの同梱漏れを
検出する所有者テストも追加した。

### LIVE記録と外貨売上の独立した準備

LIVEの3表に限定した読取りを、既存Skillの対象抽出・計画作成へ接続した。
空履歴、同じ開始で異なる終了、履歴アンカーの競合、対象ビューからの離脱、
関係先変更・不完全ページの5統合ケースが成功した。
Skillに残っていたcreator/start/endを同一性とする古い1文も、承認済みの
creator/startと終了時刻競合の規則へ合わせた。LIVEの業務コードは今回変更していない。
実端末・TikTok・LarkへのLIVE登録は未実施。

外貨売上は、[Providerと監視の残作業](foreign-revenue-provider-readiness-ja.md)を整理した。
既存ローカル監視1件は元のSkillを参照したまま保持している。
会計接続・仕訳登録・消込・監視変更は行っていない。

### 最終候補の検証範囲

10個のローカル候補アーカイブを隔離した利用側に導入し、固定ロックから再構築した。
配布106ファイルのソース一致、相対文書リンク26件、公開入口等47資源、
実行処理を呼ばない37exportの読込みを確認し、所有者・親連携の214テストが成功した。
この214件は、Lark Base 141件、Profile 19件、LIVE 37件、親連携17件の合計。
親側の独立caller/API操作チェックは別に2件成功した。

公開内容チェックはProfile・LIVEで成功。標準Skillメタデータ検証器は今回も
PyYAML不足で起動できず、成功に含めていない。環境への追加導入はしていない。
固定依存のうちProfileの内部Lark依存1.1.0は保持し、親の明示的な構成に1.2.0候補を
使用した。依存版を暗黙に統一した検証ではない。LIVEは未公開の0.0.0候補である。

| 所有者 | 今回のローカル記録 |
| --- | --- |
| Lark Base | `270da56` 限定登録・空の履歴読取り、`6f7c43f` 空の添付一覧、`c69f1ee` 配布参照修正 |
| Profile | `2761937` 画像なし登録内容を作る純粋関数 |
| 親 / LIVE | 親連携テスト・記録とLIVEの1文修正は、既存差分を保持した未ステージ状態 |

親の採用pin、正式公開、既存運用への導入、スケジュール切替は未実施。
Profile登録の承認対象と今回変更していない実行ファイルのhashは私的記録に保持した。
新規登録前なので、この作業の復旧は各ローカル記録・所有差分に限定できる。
開発Baseと既存合成Creatorは削除せず、実登録の承認がないまま再送しない。

作業枠の結了: 2026-09-08 19:51:28〜20:51 JST、約1時間。Projectの最終状態は完了2・未着手16・レビュー待ち1・Blocked 7・進行中0。変更5件と対象外21件を読み戻して確認した。外部登録は未実施で、具体的な2件計画の承認待ちを保持した。


## 登録承認後の実接続結果 — 2026-09-08 21:14 JST

ユーザーの「登録して」を提示済み2件・添付0件の計画への承認として記録し、実行ファイル4件のハッシュ一致と未実行を確認して実行した。指定の開発Botで合成プロフィール履歴2件を登録し、2件とも業務値の読戻し照合に成功した。同一観測の再計画は新規0件・登録済み2件。batch-createは1回成功し、曖昧応答からの復旧は不要だった。画像添付・更新・削除は0件。

証跡は `tmp/v2-project/second-hour-create-owner-approval.json`、`second-hour-create-result.json`、`second-hour-created-profile-readback.json`、`second-hour-profile-repeat-plan.json` に保存した。検証行は保持し、実行マーカーを消して再送しない。今後の差分は新しい計画で扱う。変更対象は実行記録とProjectであり、実装・公開版・親pinは変更していない。画像対応と配布済み手順からのM3受入は残る。Profileの当該承認待ちは解消し、残作業をTodoとして追跡する。


## 次の作業枠 — 画像照合、30分を目安

変更カード: 主分類B、補助G。主境界はLark Base Providerの既存添付読取りと履歴向け照合の接続。指定Base・テーブル・添付フィールド・現在セルへの所属・サイズ・取得前後の一致を保持する。既存の選択済み添付readerを使い、履歴一覧が返した添付だけを内容ハッシュ化する接続と拒否テストを完成条件とする。新しいアップロードやappendの外部実行は次の計画と承認の対象。実接続や運用切替は行わず、ローカル差分で復帰可能。並列エージェントなし。JavaScriptを使用（親のNode >=22と配布互換）。モデル選択方針はAstra/lowを継続し、ホスト設定を変更したとは扱わない。

### 画像照合の完了記録

Providerのローカルコミット `e32da64` に、現在セルに束縛した読取り専用の `createLarkBaseHistoryAttachmentReader` を追加した。任意のURLやコピーした添付記述子を受け入れず、フィールドID・セル所属・トークンとサイズを取得前後で照合する。複数行の並行照合は内部で直列化し、読取り予算と監査を共有しても途中結果が混ざらないようにした。選択済みtransportは専用インスタンスを渡す。

Provider全体148件、関連する親統合と拒否ケース31件、最終アーカイブから14件が成功。配布ファイルのソースとのバイト一致と公開exportの読込みも確認した。アーカイブ検証は既存の親依存を使用し、新しい固定lockでの独立インストール受入を意味しない。Profileの実際のprepare処理と選択済みCLIの引数・JSON直列化を接続した合成テストでは、同一画像の追加不要、取得中の変更・未承認originによる計画停止、不完全一覧の停止を確認した。テスト内のCLI応答と画像取得は合成応答であり実サービスへの画像アクセスではない。

途中でテスト入力のmanifest情報・画像MIME・transportの明示認可callback不足を修正した。並行処理のケースは修正前に失敗を確認してから直列化した。既存Skill実装、親pin、公開版、運用設定、開発Baseの2件は変更していない。親側には新しい統合テスト2件のファイルと、既存合成fixtureの入力参照exportだけを追加した。

証跡: `tmp/v2-project/history-attachment-provider-tests.log`、`history-attachment-integration-tests.log`、`history-attachment-archive-result.json`。復帰は当該Providerコミットと今回の親側差分に限定する。残りは画像付き新規登録のupload結果と承認したローカル画像内容の一対一束縛、および既存行へのappend契約確認。今回の読取り成功は画像登録の承認や全Profile M3の完了を意味しない。


## 画像付き新規登録の作業カード

主分類B、補助D。Providerの既存media uploadとbatch createを、承認済みの画像先行・作成時添付というSkill契約に接続する。初期範囲は画像付き1行、upload1回/create1回に限定。画像のローカルパス・サイズ・内容SHA、通常フィールド、添付フィールド、選択actor/Base、計画SHAを固定し、曖昧な書込みは再送しない。Skill側は既存payload変換を再利用する純粋なレビュー用出力だけ追加。依存順序は純粋な計画→Provider実装/合成検証→同じ開発環境の新規計画提示→当該計画承認後の実接続。次のゲートは具体的な画像付き計画の登録承認。現行no-avatarルートを保持、公開/pin/運用切替なし。並列エージェントなし。

### 画像付き新規登録の準備完了

Provider `04b834f` とProfile `6a5e834` をローカルで保存した。画像付き1件のcreate intentは、通常のフィールドと画像のパス・名前・MIME・サイズ・SHA、添付フィールド、upload/createの選択bindingを固定する。アップロード結果のtokenをプライベート証跡へ保持できた場合だけ、そのtokenを含む新規1件を作成する。失敗後の同一インスタンス再送を禁止し、実接続helperは排他的マーカーとfsyncしたイベントファイルを保持する。

保存後の画像照合には、既に操作契約を確認済みのmedia:downloadを明示選択した。前回のセル所属チェックを共用し、同じactor/Base、取得前後の所属、返されたバイト数と監査を照合する。任意のCDN originを追加する処理はない。

検証はProvider159件、Skill19件、親統合27件、配布アーカイブ39件が成功。アーカイブ54ファイルは元ソースとのバイト一致を確認した（既存親依存を利用し、新規fixed-lockインストール受入ではない）。CLIのmultipartファイル/JSON引数、作成時添付、API画像本体の読戻し、応答喪失後の再送なし照合、添付承認不足・ローカル画像変更の停止を検証した。初期合成応答に不足していた添付サイズと、検証PNGのチャンクCRCを修正して最終テスト/レビューを作成した。

開発Botのuploadとmedia-readのpreflightはいずれも成功。既存開発Baseの読取りから、新規1件・画像添付1件・更新削除0件の計画を準備した。対象はsynthetic_profile_alpha、フォロワー101、68バイトの1×1 PNG。計画SHA: `bd3c333e0251af52c7be08afb748a15b94fc05eaec097c5db5b8d0e5f60580c6`。実行意図SHA: `c5f038b23c570d463fd9d95b767dc90b9412ad7082aa9e6457e8bb2ffbba9a9f`。未承認・未アップロード・未登録である。レビューは `tmp/v2-project/image-create-review-ja.md`、実行helperは `apply-dev-image-review.mjs`。承認後はこの計画/件数に対する実際の返答を記録し、実行ファイルSHAと現在計画の一致を再検証して実行する。

不明結果は `image-create-attempt.json` と `image-create-event-*.json` を保持して読戻す。マーカーを削除して再実行しない。登録前に残ったuploadも自動削除しない。既存行append、画像付き複数行、公開、親pin、運用設定の切替はこの候補に含まれない。ProfileのProject項目はこの1件のAwaiting Review、M2残作業はTodo。


### 画像付き1件の承認実行 — 2026-09-08 22:08 JST

ユーザーの「はい」を提示済み画像付き1件の計画への承認として記録し、実行ファイル6件のSHA一致を確認した。開発Botによる画像upload1回、新規行create1回が成功。保存画像を選択済みAPIで読み、ローカル画像と内容SHAが一致することをProfileの業務照合で確認した。既存2件の読戻し値は変化せず、履歴は計3件。同じ観測の再計画は新規0・画像添付0・登録済み1。応答不明からの復旧は不要。

実行証跡: `tmp/v2-project/image-create-owner-approval.json`、`image-create-attempt.json`、`image-create-event-*.json`、`image-create-result.json`、`image-create-readback.json`、`image-create-repeat-plan.json`。マーカーと登録行・画像を保持し、再送や自動削除をしない。変更範囲は承認された開発用データと作業記録/Projectのみ。公開・親pin・運用切替なし。今回の承認待ちは解消。既存行への画像追加契約と、配布済み手順からの全体受入は引き続き残る。


### 利用枠を踏まえた優先順位 — 2026-09-08

ユーザー指定のA:招待ステータス・プロフィール、B:LIVE指標・コイン代経費、C:売上計上/入金消込・ギフト履歴、D:Baseメンテ/バックアップ/リストアを採用。G分類の管理変更としてProjectにMigration priorityを追加し、関連24件へ設定した。既存PriorityはGitHub側で編集不可だったため変更せず、専用欄を使用した。共通Lark・配布・切替の3件はAに必要な範囲のみと明記。全26件のStatusを保持し、指定のない月次完了・評価更新の2件は未割当のまま。既存行保護など個別操作の必須条件は維持。実装再開・外部データ変更・公開はなし。反映24件、対象外2件、Status26件の不変を読戻し検証した。今後はAの2業務の完了を優先する。


### チケットの構造化 — 2026-09-08

G分類。既存26件を非公開の管理repoのIssueへ変換し、既存Project item ID・本文の過去記録・Status・A〜Dを維持。完了済み2件はIssueもClosedに揃えた。一括変換がGitHub処理上限に達した際は実状態を読み戻し、未変換9件だけを処理して重複を防いだ。

GitHub標準の親子15組、blocked-by/blocking30組と7種類のラベルを設定。Work size（Complete/S/M/L/XL）とAffected repositoriesを追加し、各Issue本文にも規模の根拠・親子/依存リンク・対象ownerを記録した。残作業の相対規模であり時間・利用枠の予測ではない。リモート未設定のSkillはローカルownerとして明示した。

全体共通IssueがAをB〜D待ちにしないよう、A向けLark対応・配布確認・切替の3子Issueを追加した。最終29件のタイトル・本文・ラベル・フィールド・親子/依存の両方向を読戻し照合し、循環がないことも確認。元26件のStatus/優先順位を維持。記録はtmp/v2-project/structure-plan.json、structure-verification.json。開発・公開・本番切替は実行していない。

### A再開: 招待可否用の単一選択値 — 2026-09-08

変更カード: 主分類B、Providerの既存selected create境界のみ。Issue #30の一部として、既存SingleSelect選択肢に限定した登録意図を実装・検証する。未知値による選択肢追加、曖昧な選択肢、レビュー後のフィールド変更は停止する。完了条件はProviderの正常・拒否テストと直接Profile回帰の通過。次の実接続は独立した具体的計画・承認が必要。外部業務データ変更・親pin・公開なし、対象差分のみ取り消し可能。並列委譲なし。既存の画像付き作成証跡を再利用し、append専用APIは検索とCLI helpだけでは仕様が確定しないためunverifiedを維持する。


Provider `197c424`で実装完了。既存選択肢名の厳密一致だけを許可し、ID/名前の欠落・重複と実行直前のproperty変化を拒否。APIに原子的なschema条件はなく、最終読取り後の並行変更は読戻しで確認する限界をknowledgeに記載。Provider create/image、親Profile create/image統合、独立caller inventoryの計41テスト成功、対象diff確認済み。Issue #30本文の反映を読戻し照合。業務データ・運用・公開・親pin変更なし。次は招待履歴のselected read/create組合せと承認済みeligibility専用入力の統合。Issue #30全体は未完了。


### 30分再開: 招待同期の選択済み更新経路 — 2026-09-09

主分類B（既存操作の制限付き組込み）、副分類D。Providerの日時更新境界と招待Skillへの直接統合に限定。承認済み観測と一致する履歴の日時だけを前進させ、既存行の内容・参照先・添付を保持する。最初の完了条件は選択済み日時更新adapterと拒否テスト、次に同入力によるSkill統合。実データへの適用・公開・親pin・切替は別gate。既存履歴を意味変換せず、未知結果を招待不可へ変換しない。焦点テストと差分確認で検証し、対象差分を戻せる。委譲なし、既存の選択経路と証跡を再利用。

採用済みeligibility専用境界のD移行として、新しい型付き入力を旧state入力から分離。宛先/フィールドを束縛したversion 3計画、実履歴の意味を毎回明示的に確認するcallback、not_found/unavailableをnullのまま保持して全体停止する処理を追加した。BackStageも新規取得時に明示選択した場合だけ型付き出力を生成し、招待種別を可否へ混ぜない。歴史データや旧計画の自動変換なし。現在画面を新規検証したとの主張はしない。

記録したownerはLark `90ce5f0`、招待Skill `021222b`、BackStage `43665d4`。Provider 169、Skill 22、BackStage 26、親の直接統合/独立inventory 26テスト成功。画像なし新規追加、日時だけ更新、読戻し、応答喪失後の再送拒否、フィールド/内容変更と意味不明履歴の拒否、Private Source→型付きPublic Skill→選択済みCLIの合成データ統合を確認。3つの未公開archiveの74ファイルがsourceと一致し、archiveから66テスト成功。依存は既存開発環境を再利用し、新規fixed-lock再インストールとは区別する。

選択済み開発Botのupdate preflight成功。開発Base一覧3表の読取りで招待履歴表が未作成と確認。private schema案は `tmp/v2-project/invitation-30min-development-schema-plan.json`。Creatorは単一値DuplexLinkが必要だが、CLI shortcutのlink schemaはmultiple=falseを記載していないため、勝手にpropertyを足したり複数値で代替しない。既存Text型アカウントをそのまま扱える局所互換も検証した。新テーブルが逆向き関連列を作る影響を案に明記。今回の業務データ変更は0件、承認消費なし。

証跡は `invitation-30min-*-tests.log`、`invitation-30min-archive-result.json`、`invitation-30min-development-preflight.json`。Issue #6/#14/#30に現在候補と残条件を反映。元からの未ステージ文書・依存更新・テストを保持し、Skillは新コード/テストと追加exportだけをcommit。今回の文書差分と親統合テストは既存作業ツリーに保持。親pin・公開・運用・schedule切替なし。復旧は対象候補の採用を止め、既存routeを維持。次は正しい単一関連を持つ開発履歴schemaの準備と、具体的な登録/更新計画のレビュー。

最終確認: 3件のIssue本文をローカル案と完全照合。未公開archiveから新しい6つの公開entry pointも正常import。既存の開発プロフィール用単一関連作成receiptはtype 21 / DuplexLink / multiple=falseを示すため、必要なcardinality自体は以前の実接続で確認済み。次回はその証跡を参照し、招待用の新しい具体的schema操作を組み立てる。旧作成helperや承認は再実行・流用しない。


### 約1時間の再開 — 2026-09-09 00:34 JST

A優先を継続。最初はA/F分類で、公式仕様を根拠にtables:createのProvider知識を追加し、選択済み開発App/Bot/Baseへ空の招待履歴表を作る。既存の開発Base作成・利用の許可を適用する。影響は新表7列とCreators側の逆関連1列で、既存レコードの値は変更しない。実行前の一覧/フィールド照合・一回限りの送信・attempt記録とschema読戻しを完了条件とする。不明結果は再送せず記録を保持。新規履歴と日時更新は次の具体的Skill計画の承認gateに分離。委譲・公開・親pin・運用変更なし。

### 開発用招待履歴の作成・登録確認 — 2026-09-09 00:53 JST

- 空の開発用招待履歴テーブルを作成。7項目と逆関連1項目、既存5レコード保持を読み戻して確認。テーブル作成準備の最初の実行は必須binding hash不足により送信前に停止した。選択transportの制御順を確認し、未送信の停止記録を保存した上で別attemptとして実行し成功。応答不明の書込みを再送したものではない。
- ユーザーが計画 `2df508aaec0537314616253d6f41d02eede64ed1d428b8fc410de1795c565586` の新規2・時刻更新0・添付0・登録済み0を明示承認。選択済み開発App/Bot/Baseで架空のalpha/beta履歴2件を1回のbatch-createで作成し、Skillの業務再読取で完全一致を確認。同じ観測の再計画は追加0・更新0・添付0・登録済み2。実TikTok観測の検証ではない。
- Lark Providerのテーブル作成契約と知識を `7d52a25` にローカル保存。全169テスト成功、独立caller/API inventory 2テスト成功。公開・親pin採用・運用切替なし。
- 私有証跡: `tmp/v2-project/invitation-hour-table-{intent,response,readback,verification}.json`、`invitation-hour-create-{plan,intent,approval,dispatch,response,result,readback,replay}.json`。登録済み履歴を保持し、作成スクリプトは再実行しない。
- 着手時のProject更新漏れをユーザー指摘で修正。#14をIn Progressにしreadback確認、#30にスキーマ作成結果を記録。次は実登録2件を使う日時のみ更新の別計画。今回の承認を流用しない。

### A優先・既存セル画像追加の知識補完（同時間枠）

主分類B、副分類A。境界はLark Providerの既存attachments:append未検証知識と、選択済み既存履歴への画像追加候補。既存履歴・添付を保持し、追加する1画像のbytes/hashと対象セルbaselineを固定、応答不明時は再送しない。まず同一版の公式CLIソースで送信契約・認証種別・scope宣言・テスト応答を確認し、候補と停止テストを準備する。外部画像追加は別途具体的計画承認が必要。TikTok取得・運用切替は含めない。並列agentなし。

公式 `larksuite/cli` v1.0.93（commit `2aebe8970f0a472dfc864b6ac3d19d080e75041f`）の画像追加処理を取得。User/Bot、shortcutのscope3種、attachmentsのrecord/field/token形式を確認。これはネイティブAPIの最小scopeや既存画像保持の実接続結果までは立証しない。#14は日時更新計画のAwaiting Review、#30は画像追加のIn Progressへ更新。

### 今回の区切り — 約42分、2026-09-09 01:17 JST

実施済み：開発招待テーブル作成と既存5件保持、承認済み架空履歴2件の作成・業務readback・追加0件の再計画。Providerの所有者checkpointは `7d52a25`（テーブル作成根拠）と `67f5cbf`（画像追加の公式CLI調査と未解決gate）。Provider作業ツリーはclean。親pinや既存staged/unstaged変更は採用・整理していない。

検証：Provider169テスト、独立caller/API inventory2テスト。過去に検証した招待・プロフィールのアーカイブで実開発応答の登録前後を再現。A候補5件の静的配布閉包は104ファイル・41export・72相対import・27相対文書リンクが欠落なし。最後の知識追記はLarkだけ再packし、配布された文書bytesを照合した。静的検査の最初の2候補は文字列内のimport語を誤検出したもので、検査式の修正後は欠落なし。根拠URLで停止した画像追加下書きは成功テスト数に含めない。

未承認・未実行：日時更新計画 `a084f7e741a2d48f8325f02fa77b505e4c2408329fded1cd68bec105e0955b7d`（新規0・更新2・添付0・登録済み0）。approval receiptとdispatchは存在しない。公式CLI固定版ソースを根拠として認めるかは [判断用メモ](lark-append-evidence-source-ja.md)への回答待ち。画像追加operationはunverifiedのまま、下書きは公開export・配布物から除外。

Project #14/#30のAwaiting Reviewと、#31のTodo（準備完了・先行受入れ待ち）を本文とともにreadback確認した。In Progressを残していないのは、ここでは実際にレビュー回答待ちだからである。1時間は目安だったが、独立したA準備を終えたため約42分で区切り、検査の繰り返しで時間を埋めない。

再開：日時更新が承認されたら、保存済みのhash/件数に対応するapproval receiptを作り、`apply-invitation-development-update.mjs` を一度だけ実行する。途中で止まった場合はattempt/dispatch/responseを確認し、無条件に再実行しない。画像追加は根拠条件の決定後に別変更カードで共有契約を扱うか、公式endpoint文書の探索を継続する。今回の作成済み2件は削除せず保持する。


### 個別承認済み日時更新の完了

所有者が提示済み計画 `a084f7e741a2d48f8325f02fa77b505e4c2408329fded1cd68bec105e0955b7d`（新規0・日時更新2・添付0・登録済み0）を「承認します」と承認。実行前にアカウント・項目・選択肢・履歴baselineを再検査し、選択済み開発Botで1回のbatch-updateを実行した。業務readbackで2件一致、日時以外の全field保持、再計画で追加0・更新0・添付0・登録済み2を確認。応答不明や再送はなかった。

私有証跡は `invitation-hour-update-{approval,attempt,dispatch,response,result,readback,replay}.json`。日時更新の承認待ちは解消。#14全体の実ソース・avatar・workflow受入れは未完了のためDoneにせずTodoに戻す。次の所有者レビューは#30の根拠条件。公開、親pin採用、画像追加、運用変更は含めない。


### 招待可否の地域表示修正と9件の再現検証 — 2026-09-09

変更区分B。所有者が選択・目視確認した9件を根拠として、私有BackStage Provider 5890962に地域制限の完全一致ラベルを追加した。知識版は2026-09-09.2。地域を推定せず、型付き入力の観測値として保持する。不明結果を対象外へ変換せず、旧契約の変換も変更しない。親はこの記録済み子版と混在入力の停止検証を採用する。

実際の確認：Provider全体と独立caller inventoryで29成功、親の混在統合とSkill型付き契約で13成功。未公開archiveから、入口・関連知識・変換実装の計6資源のbytes一致と知識版を確認。最初のpackは標準cacheへの書込制約で失敗し、作業専用一時cacheで成功した。依存追加・パッケージ公開・ホスト登録はしていない。

保存済み9件の再現では観測7、不明2を保持し、対象を減らさず全件停止、宛先アクセス0、作成/更新/添付すべて0を確認した。再現用record IDは仮の値であり実行計画ではない。続いて同じ内部タブで9件を再検索し、前回の表示と一致した。7件の行画像はページの資産一覧と一致する項目が0件だったため取得・採用していない。非公開証跡は tmp/v2-project/invitation-region-replay/ に保管し、署名URLを永続化していない。所属ヘッダーの独立確認、数値ID、画像バイナリ、宛先の実受入れは未完了。招待送信・フォロー・Lark更新は0。

AI方針レビュー：知識方針・開発方針・Private Source Integration Guideに照らし、実差分、既存型付き契約、配布知識、旧版との境界を確認した。現在statusに残っていた「対象指定待ち」を是正し、過去の観測と今回の再現・再検索を区別した。実アカウント/画像/署名URLを公開SkillやGitへ追加していない。セルフレビューであり、独立レビューや人による引継ぎ理解の証明ではない。

復旧は親pinと対応文書・統合検証の差戻し。旧版では地域表示が再び停止する。運用データを変更していないためデータ復元は不要。

| 次の作業 | 人の担当 | 期限 | 概要・完了条件 | 参照 |
| --- | --- | --- | --- | --- |
| 取得引継ぎの残件 | TBD | TBD | 所属確認と正確な画像取得の制約を解消し、根拠を残す | #6 |
| 招待ワークフロー受入れ | TBD | TBD | 不明2件の意味を維持し、宛先検証と必要な具体的登録計画を分離する | #14 |
| 人の引継ぎ確認 | TBD | TBD | 配布文書から判断・停止条件を辿れることを人が確認する | #39 |

### Lark共通の経路順序の是正着手 — 2026-09-09

所有者は、Base/Chat共通で①API、②ブラウザー、順次フェールオーバーという既存指示を再確認した。新仕様の採用承認は不要。主区分Gとして、その指示を開発方針に明記した。次の実装是正はE/Bとして、両Providerの経路選択・失敗分類・呼出し元を対象にする。並列agentなし。

確認した不整合：Baseの access-policy.md はlimited/unconfigured時にブラウザー優先を規定し、src/index.js と src/profiles.js に call-minimizing が残る。Base/Chatの等価経路判定は存在するが、判定だけで自動の順次実行・実ブラウザー受入れ済みとは言えない。過去のブラウザー操作記述は現在のv2構成の一連のフェールオーバー実証と区別する。#30の既存API受入れは取り消さず、代替経路の実証を追加の未完了項目として扱う。

指定テーブルの読取りを所有者が明示許可した一方、内部ブラウザーは管理ポリシーで接続を拒否した。プロジェクト設定はdefault origin denyで指定Lark originの許可がなく、拒否と整合する。実効設定の全層は未確認。拒否には別経路の迂回禁止が含まれるため、このターンでCLI/OpenAPIの取得、認証変更、許可設定変更は実施していない。LarkのAPI権限不足と診断したものではない。ステータス全件照合は未完了。

AI方針確認：所有者の再確認した順序を根拠にし、旧文書の逆順を正当化しない。権限拒否・結果不明の変更に対する既存停止条件を維持し、方針の記録だけで実装完了を主張しない。復旧は今回の文書差分の差戻しで足り、外部データ変更はない。
