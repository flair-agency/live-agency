# 優先Aの移行差分監査 — 2026-09-10

現在の到達点は後半の「永続保存と画像なし履歴の再開接続」と「種別情報の最終採用指示の回収」。それ以前の実装未完了の記述は各時点の記録です。

対象は招待可否記録・プロフィール記録、および直接の取得Provider。履歴整理Skillは対象外。分類G、読取り比較と記録のみ。動作変更・本番通信なし。

# 比較根拠

旧配置 ~/.codex/skills/creator-invitation-status-sync と creator-profile-sync のコア・Lark runtime・入力仕様を現行ソースと比較。旧配置は当時の実装資料であり正本の代わりではない。招待の元ユーザー説明は前作業で取得したタスク 01a037c9-4516-7ce3-9061-70fda7e4562b。採用判断は remaining-m3-contract-decisions-ja.md、domain/model.md と照合。プロフィールProviderはM1時点67b39ebのsurface文書とも比較した。

# 結果

| 項目 | 所見・影響 | 判定 |
| --- | --- | --- |
| 招待ステータス意味・表示された検索不一致 | 既知7表記・内部分類の知識漏れと誤停止は4d72c97までで修正済み | 既知不具合、修正済み |
| 招待の一般／プレミアム | 新typed経路は種別をeligibilityから除外し、Provider出力に種別の保存先がない。別概念として扱うだけでは旧版の種別保持と同等にならない。私有観測証拠への保持指示はあるが、記録先までの保持は未確認 | 情報保持の不足候補。設計上の分離と情報喪失を区別して追跡 |
| 招待の100件上限 | 旧runtimeは100件単位で繰返し処理。新buildInvitationHistoryWritePayloadsは各操作101件以上を拒否。exporterの既定limitはnullであり到達可能。既存文書に実装制限の記録はあるが、旧能力との同等性は未達 | 選択された新compositionの能力差分 |
| 招待の履歴レビュー | reviewHistoryが新規必須で読戻しにも適用。履歴の意味を確認する方針は所有者承認記録があるため、無断の知識追加と断定しない。親子レコードを同一に潰す比較はない | 承認済み方針に由来。callback実装の適合は別途必要 |
| 両Skillの比較コア | 旧配置とのdiffはvalidator import先のみ。招待の完全一致比較・タイムスタンプ更新・差分追加、プロフィールの比較アルゴリズムは保持 | 比較した範囲で追加の変更なし |
| プロフィール入力仕様 | 旧配置との文書diffなし。値欠落とゼロの区別、観測値の整合・時刻の取扱いを維持 | 比較した範囲で漏れなし |
| プロフィールruntime | import置換と新payload構築関数を追加。既存計画・apply本体の差分なし。新composition100件制限は既存exporterの最大100件と整合 | 通常の選択範囲で新たな制限差を確認せず |
| プロフィールProvider | 現行surfaceとM1 67b39ebのsurfaceは一致。投稿範囲・固定投稿・写真・日時根拠・欠落・アバター手順を保持 | M1からの知識脱落は確認せず。M1以前の全会話網羅ではない |

# 検証と限界

両SkillとTikTok Web Providerの63テスト成功。ただしテスト成功を旧仕様一致の証明に代用していない。今回の根拠はコードと文書の差分。全過去タスクの網羅、実運用の全入力、プロフィールのM1以前の全知識、実ブラウザでの再取得は未確認。問題なしという全面的な受入判定はしない。

AI自己レビュー: 知識・開発・言語方針に沿い、確認した差分、承認根拠がある方針、未確認事項を区別した。実データ・認証情報は含めない。変更は本記録のみで取り消せる。

| 次の作業 | 人の担当 | 期限 | 完了条件 |
| --- | --- | --- | --- |
| 招待種別保持の追跡 | TBD | TBD | 別項目へ分離する採用判断と旧情報の保存先を対応付け、欠落を解消 |
| 招待101件以上の能力差分 | TBD | TBD | 旧バッチ能力と選択compositionの対応を示し、単なる移行としての制限追加を解消 |

# 処理単位の仕様訂正 — 2026-09-10

所有者の指示に基づき[処理単位とチェックポイントの仕様](../architecture/execution-batches.md)を策定。BackStage取得30件、宛先への送信100件（API上限の主張ではない）、Runtimeの検証済み進捗を分離した。総件数による拒否は認めず、全体計画の承認と分割送信を分離する。250件なら取得30件×8＋10件、作成250件なら保存100＋100＋50件。途中再開は同一計画・現在の宛先・完了効果を照合し、結果不明を再送しない。

変更カード: 主分類E、副分類G。今回は親リポジトリの仕様のみ。次の実装は選択writerの複数batch構成、公開Skill payload、Runtimeの永続checkpointをこの順序・契約で接続する。既存single-batch制限は未修正。実装完了条件は仕様の境界件数・中断・応答不明・画像残件試験。人の担当・期限TBD。仕様撤回は文書差戻し、本番復旧は不要。

AI方針レビュー: 既存の承認と効果順序を維持し、取得上限・送信単位・業務対象総数を混同しないことを確認。チェックポイント保存だけで新たな業務承認を要求しない。同一計画を継続できない場合の残件計画は既存承認規則に従う。図と手順、ローカルリンクを確認。動作試験は未実施（製品コード変更なし）。

# 実装着手 — Runtime基礎部分

#6をIn Progressへ変更。Runtime dede2d2（codex/invitation-checkpoints）に明示的な計画・binding・承認・永続化・読戻しhookを受ける再開executorを追加。送信前のattemptを保存し、再開時は結果不明を再送せず照合する。12テスト成功、独立caller inventory成功。未知結果と保存失敗は後続停止。

未完了: Lark writer接続、request分割、実ストレージと排他、画像段階の統合、種別保持。したがって100件制限はまだ解除されていない。親pinは開発組成として採用し、本番・公開・登録は行わない。AI自己レビューでは実装範囲と未接続の限界、秘密情報なし、既存承認の保持を確認した。復旧は旧Runtime pinへの差戻し。次は同一計画に束縛した複数writerの接続（担当・期限TBD）。

# 複数バッチ保存の接続検証 — 2026-09-10

主分類B（仕様採用後の実装）、Providerの複数バッチintentとSkillの全体payload、親の統合テストを一つの範囲として実施。Lark Provider d1523b5、知識版2026-09-10.1、Skill 4a9a7dc。1つの計画に各batchを束縛し、全更新→全作成→既存画像→新画像の順序を保持。新経路で250件を100/100/50として保存・読戻し。既存v1 writerは互換のため保持。

Provider全体・Skill・混合統合・独立caller検査の251テスト成功。101/250件、第二batch応答不明で後続停止、イベント保存失敗、計画不一致、重複child、混合画像順序と事前の画像bytes検査を含む。初回はテストのtransport payload名がbodyとなっていたためjsonに訂正。混合試験では単一intent引数の混入を検出し、親builderが選択情報だけを受け渡すよう修正した。

AI自己レビュー: 全体承認・各要求の一致・既存読戻しを保持。公開Skillへ私有ラベルや取得制限を追加しない。差分検査成功。Skill形式validatorは環境にPyYAMLがなく未実行完了（依存追加なし）。

未完了: 永続checkpointとLark経路の接続、再起動時の画像残件照合、種別保持、実運用受入。新writer単体は停止後再利用不可であり、同じ計画を新インスタンスで再送してよいという意味ではない。#6はIn Progressのまま。次はRuntimeの実ストレージ・排他・宛先照合を接続する（担当・期限TBD）。復旧は前Provider/Skill pinへ戻す開発変更。本番変更・公開・マージなし。

# 永続保存と画像なし履歴の再開接続 — 2026-09-10

変更カード: 主分類B、副分類E/G（採用済み処理単位仕様の具体化）。Runtimeの保存・排他とLark Providerの宛先照合、親の統合試験が対象。全体計画・現在の承認・既存子writerの事前確認と読戻しを保持する。新規の実行環境、実データ操作、公開・マージは選択しない。今回の完了条件は画像なし作成/時刻更新の保存・再開試験と開発ブランチ同期。画像再開とSkill全体の業務再計画接続は次の範囲。並列agentなし、実行中モデル設定は変更していない。

Runtimeに明示的な私有保存先とrun IDを受ける保存処理を追加。送信前attempt、応答ID、検証済み進捗を原子的に保存し、同一runを排他。強制終了後のlockは自動削除せず、同じtokenと終了済みPIDを確認する明示回収を設けた。回収で宛先やcheckpointを消さない。PID再利用・壊れたlock・回収中断は手動調査が残る。

Lark Provider知識版2026-09-10.2の候補adapterは、承認済み複数batchを既存writerへ渡す。作成済みIDを読戻し前に永続化し、再開時に作成内容・fieldを照合。時刻更新は変更前baselineではなく承認済み変更後の行で照合。101件の先頭100件、250件の先頭200件で中断しても、照合後は残りだけを作成した。完全な応答喪失でIDがないcreateは未解決のまま止まり、似た値による推測照合・再送はしない。画像付き計画はこの候補adapterでは書込み前に拒否し、既存画像経路から黙って省略しない。

検証: Runtime全体・Lark Provider全体・新しい保存接続・既存混合Skill接続・独立caller検査の270テスト成功。初回集約では知識版の固定期待値1件が旧版のままだったため更新し、再検証。プロセス強制終了試験は実際の子プロセスへSIGKILLし、シグナル終了とattempt残存を確認。外部APIを使わない合成試験であり、実Larkの再開受入ではない。

AI方針レビュー: 知識/開発/言語方針に基づき、図と実装の順序、保存情報の私有性、同一承認の保持、既存読戻しの再利用、実装と未完了の境界を確認。Runtimeの旧説明（永続保存未接続）を現在の範囲に更新。公開Skillは変更せず、Lark固有の照合知識は私有Providerに置いた。文書から人が保存場所・既実行/不明・再送禁止・lock回収の意味を辿れることをAIが確認したが、人の引継ぎ演習を完了したとはしない。

残件: 画像アップロード/添付の再開、取得batchの保存、Skillの履歴レビューを含む全業務経路との再開接続、種別保持、実運用受入。現在は開発用の明示compositionであり、配布済みRuntime/Providerの旧版で新exportが使えるとは扱わない。リリース時は両所有者の対応版を揃える。戻す場合は今回のRuntime/Provider pinと統合テストの変更だけを差し戻す。本番復旧処理は不要。

| 次の作業 | 人の担当 | 期限 | 完了条件 |
| --- | --- | --- | --- |
| 画像と業務再計画を含む再開接続 | TBD | TBD | 中断した段階と残効果を保持し、全Skillの履歴レビュー/全体読戻しを通す |
| 種別保持の根拠と保存先の追跡 | TBD | TBD | 過去の所有者説明と現契約を照合し、一般/プレミアムの情報喪失を解消 |

開発checkpoint: Lark Provider `8064ce2`（codex/invitation-multi-batch）、Runtime `b941ebc`（codex/invitation-checkpoints）。両所有者を先にremoteへpush済み。親pinは候補の組成として採用する。


# 種別情報の最終採用指示の回収 — 2026-09-10

TSVタスクの元ユーザー発言を時系列で再確認。2026-08-27 02:50 UTCには追加レスポンス情報を保持する探索要望があるが、11:26には一般/プレミアムを子ステータスで表す構造を採用し、11:31にはUser ID以外の追加レスポンス情報は現状保持不要と指定、11:32〜11:33に要件確定・実装指示が続く。したがって生の種別配列を新フィールドへ保存することが最終要件だったとは扱わない。

現在のtyped出力は種別fieldがなく、一般/プレミアムのどちらも親の対象になることを既存Providerテストと出力validatorで確認。概念分離の承認と情報破棄の承認を混同してはならない。子ステータスに含まれていた情報を、どの明示表現で宛先まで保持するかが実装残件。A〜Eの自動推測を追加する話でもない。

BackStageの私有知識版2026-09-10.3へ最終指示の順序・人の確認例を反映。3=一般、6=プレミアムという当時のassistant報告も発見したが、元の実証対照を今回再検証したわけではないため「公式に確定した数値契約」とせず歴史的な実装根拠候補と明記。新たな内部APIアクセスを実施・許可していない。

BackStage全体・招待Skill・独立caller検査の53テスト成功。Runtime/Lark/BackStageのpack dry-runで新しいコードと知識文書の同梱を確認。配布・稼働環境への導入ではない。AI方針レビューでは途中案と最終採用指示を区別し、データ保持の新要件を作らないこと、歴史的assistant報告の確度を明示することを確認した。次は情報保持の表現と既存子ステータスとの対応を具体化する（担当・期限TBD）。

BackStage知識checkpoint: `9b8c365`、codex/invitation-region-handoff、remote同期済み。

CI追跡: 親c191aa0のGitHub CIで、古いsource-handoff試験が表示された「見つかりません」を不明結果と期待していたことを検出。製品実装は戻さず、既知ラベル2件の型付き記録/読戻しと汎用不明結果2件の停止を分離して4テスト成功。全体検証を再実施し、残る1失敗はnpm標準cacheへのsandbox書込み拒否だった。fixture配布試験だけ一時cacheで再実行して成功。cacheの所有権変更や依存追加は行っていない。

# 責任境界の解釈案 — 2026-09-10（承認待ち）

所有者の説明を受けたAIの解釈案は、ProviderがBackStageのステータスと招待できる場合の招待区分を別々に渡し、ConsumerであるSkillがそれらを「対象」とその子「対象（一般）」「対象（プレミアム）」へ対応付けること。前段の「どの表現で残すか」という曖昧な残件は、この分担を前提に具体化する。Providerに記録先の子ステータス選択を任せる修正は行わない。

[業務モデル内の承認待ち提案](../domain/model.md#invitation-observation-and-history-classification-ownership-pending)へ記録。残件は別項目としての招待区分handoff、Skillが所有する階層対応と履歴比較、区分がない場合の親保持を同じ入力で検証すること。既存v1の消費者互換を保ち、未実装の状態で旧情報保持が回復したとは扱わない。多重アカウントの内部細分化を、招待可能な場合の表示区分と同じ事実として推測しない。

AI方針レビュー: 今回の所有者説明からの解釈として、事実の取得/正規化と業務分類の責任を分離。図の受渡しが種別を保持することと、既存実装の欠落が残ることを明記した。所有者から、こうした解釈こそ承認を求めるよう指摘があったため、採用済みという扱いを取り消した。責任分担の解釈を確認するまで依存する実装は進めない。既存の独立した再開処理の開発承認を取り消すものではない。

全体検証: 一時npm cacheを明示して再実行し、1,057テスト成功。標準cacheの権限や内容は変更していない。境界提案の承認は未取得であり、文書上もpendingへ訂正。
