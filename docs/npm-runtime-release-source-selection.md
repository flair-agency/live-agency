# npm runtime release source selection

2026-09-07 JST. **Local selection checkpoint complete; an isolated source commit is ready for owner acceptance.** Select 82 Skills paths: the accepted 75-path union plus seven exact inherited prerequisites. The proposed commit changes 77 paths; five clean payload inputs are verified without a diff. No source, index or ref was changed, and no release commit or tree ID exists yet.

Primary G, supporting future E source identity. Protected boundary: Skills release-source identity, with private root qualification evidence separately owned. Preserve accepted dirty provenance, source-neutral payloads, explicit authority, and the active candidate's staged/unstaged distinctions. One package produces this report and a private non-executable selection/patch proposal. The next gate is acceptance of this exact envelope before isolated commit creation. Complete authoritative governance guide, applicable AGENTS, task policy, accepted formal contract/checkpoint and readiness §§3/4/6 were read. Configured settings were retained; no override, delegation, successor or cross-task message.

## Concrete proposal and evidence

Owner-only generation: [release-source-selection-20260907-01](/Users/naokikimura/workspace/.live-agency-development/runs/release-source-selection-20260907-01), created through scoped filesystem review, directory mode 0700 and files 0600.

| File | Purpose | SHA-256 |
| --- | --- | --- |
| [selection-manifest.json](/Users/naokikimura/workspace/.live-agency-development/runs/release-source-selection-20260907-01/selection-manifest.json) | Exact 82 paths, current bytes/modes, HEAD/index/worktree blobs, owner, formal preimages, prerequisite evidence, source-to-payload map and separate root identities | `70fe58b36ca87fead63384735300a4a76fa33c022ae421c3f36ec2dc1fde85e6` |
| [skills-source.patch](/Users/naokikimura/workspace/.live-agency-development/runs/release-source-selection-20260907-01/skills-source.patch) | Complete 322,697-byte Git patch from the named Skills HEAD, including inherited changes and complete new files | `5f4d10138ba837e18c5cd178adb7b06e30b305c1afec663af13478bcaf9b5571` |
| [changed-paths.json](/Users/naokikimura/workspace/.live-agency-development/runs/release-source-selection-20260907-01/changed-paths.json) | Exact 77-path future commit selector, derived from actual Git state | Membership is verified against manifest and patch |

The manifest is data, not an approval or executable release configuration. It deliberately leaves intended commit/tree IDs null. Suggested isolated branch: `codex/npm-release-source-selection`; proposed message: `Bind formal npm release sources and inherited prerequisites`. This is one Skills commit, not a root/submodule-pointer commit.

Fresh observations match readiness:

| Owner | HEAD | Index SHA-256 | State |
| --- | --- | --- | --- |
| Skills | `e78ec0962184fee2b07ea85b3f24545438f232d4` | `60e240d69413599086ea6077734b1510d52e422843a6fcb32191df809c12d353` | detached; staged diff empty |
| Private root | `6b1f11c17cec53f2adcce5fd88b87666c322f859` | `91b2825ae7f22db1cb5cea37e2c85627f4c3917edcd28779f250a8e4cdcbf7ab` | `codex/sep1-independent-development`; staged diff empty |

The 82 paths comprise **67 tracked unstaged, 10 untracked and five tracked clean**. Every selected index entry equals its HEAD blob; absent entries are recorded as null. Worktree blob IDs are calculated from Git blob framing without writing objects. File modes are part of the proposal. The accepted implementation's `new` flag is retained only as historical metadata, never as a staging selector.

## Exact envelope and inherited prerequisites

Starting selector remains exactly the 67 Skills-owned entries of [formal final-manifest.json](/tmp/formal-distribution-preimages-20260907/final-manifest.json), union the 18 unique payload sources in the accepted snapshot: **75 distinct paths, 70 changes and five clean inputs**. All 67 accepted current hashes match. Final-manifest SHA-256 remains `b534dffc61bc89bcfd865ae57997396297a68c039a73409ea9502562672bc060`; its linked original `before.json` hash was also verified. No blanket inventory of other owners was repeated.

Add exactly the following seven paths, relative to the Skills Git root. None joins the three npm payloads.

| Additional path | Git state | Why its complete current bytes are required |
| --- | --- | --- |
| `fixtures/coin-purchase-instruction-provider/descriptor.json` | untracked | The accepted portable fixture manifest declares it, and the private artifact harness copies it. HEAD lacks it. Hash matches the accepted synthetic Provider archive payload. |
| `skills/_shared/lark-base-client.mjs` | unstaged | Selected callers import selected-client factories absent from HEAD. Keep the inherited explicit-authority checks and separately named legacy entry. |
| `skills/_shared/row-archive-receipt.mjs` | untracked | Both selected row compaction callers statically import it; HEAD lacks it. Exact bytes match accepted row-storage final-manifest. |
| `skills/creator-invitation-status-compaction/scripts/invitation_compaction_core.mjs` | unstaged | Selected caller needs version-2 plan admission, strict stored-value validation and lossless text hydration. HEAD would restore incompatible earlier behavior. |
| `skills/creator-insight-sync/scripts/insight_sync_core.mjs` | unstaged | The selected insight flow needs the execution-binding digest included in context hashing and validation. |
| `skills/gift-history-sync/scripts/gift_projection_authority.mjs` | untracked | Static dependency of the selected projection caller; validates selected Provider binding, review content, expiry and approval. HEAD lacks it. |
| `skills/lark-base-backup-retention/scripts/composite_retention.mjs` | untracked | Static dependency of the selected retention planner; its drill-core dependency is already in the 75-path union. HEAD lacks it. |

These are prerequisites of the requested whole selected source envelope, not extra features or an attempt to reproduce all historical tests. Artifact assembly and the four focused test files alone do not require the six non-coin prerequisites. Omitting them from the 75-path namespace selection would nevertheless leave selected source imports or accepted semantics inconsistent.

The manifest records full SHA-256/blob identities for every addition. Exact hashes for insight core, invitation core and composite retention match their existing bounded caller inventories; row receipt matches its accepted final-manifest. Their interpretation is supported by [insight conformance](../provider-runtime/docs/v2-insight-caller-conformance.md), [invitation compaction inventory](../provider-runtime/docs/v2-invitation-compaction-caller-inventory.json), [native backup conformance](../provider-runtime/docs/v2-native-backup-caller-conformance.md) and [row-storage acceptance](v2-development-coordination.md#2026-09-07-row-storage-implementation-acceptance). Gift authority is inherited from [gift projection checkpoint](../provider-runtime/docs/v2-m2u-4a-gift-projection-checkpoint.md). The shared client accumulated later caller-family factories after the original insight checkpoint: its current exact hash is bound here, without falsely treating an older inventory as its current byte receipt. The gift authority and shared-client current hashes require acceptance as part of this concrete proposal; no new implementation was authored.

Likewise, the original 75 paths retain earlier portable runtime, backup contracts, business and v2 caller work. The manifest distinguishes formal pre-namespace SHA-256 from HEAD SHA-256 where available. [Formal implementation acceptance](environment-cleanup-coordination.md#formal-candidate-implementation-accepted--2026-09-07) accepts the resulting namespace/qualification state; it does not attribute every full-file delta to that worker. A namespace-only patch against HEAD would lose inherited behavior.

## Bounded reconstruction and qualification closure

**Three artifacts:** the accepted 18 source files plus assembler/map are sufficient source inputs for the 5/3/12 ordinary-file payloads. All 18 current hashes equal snapshot inventory hashes and their assembly/receipt payload entries. The map and assembler identities also match. No extra runtime dependency is missing and no release-code repair is required. Formal package imports are Node built-ins, relative files inside the mapped payload, and coin's exact two Flair dependencies.

**Standalone focused tests:** retain these four already selected files: `test/provider-api.test.js`, `test/private-runtime-files.test.mjs`, `test/coin-expense-reconcile.test.mjs`, and `test/npm-distribution-payloads.test.mjs`. The first three are the inherited 21 direct tests; the fourth supplies nine payload tests. The payload test requires a separately selected absolute synthetic `FORMAL_TEST_ROOT`. Coin/direct API discovery reads the Skills aggregate's declared dependencies, so the nine selected fixture manifests and their existing modules/instruction resources must remain available in a future isolated dependency graph. The explicit library workspaces, fixture workspace list and accepted lock are already selected. Do not run the aggregate `node --test` discovery as a substitute for this exact four-file selection.

All 11 non-manifest fixture module/instruction/knowledge files are already clean HEAD content. No additional fixture source change is needed except the portable descriptor above. The manifest binds those 11 hashes plus three clean recursively referenced modules: weekly application core, gift projection core and invitation dual-run helper. These **14 baseline prerequisites** stay at HEAD; they are not additional commit changes. Static literal relative imports across the selected envelope resolve to selected bytes or these bound baseline modules. This is bounded source analysis, not arbitrary dynamic-program execution or a new test pass.

**Private cross-repository qualification:** the root harness remains at `scripts/npm-runtime-formal-distribution.test.mjs`, SHA-256 `b2b4690087a1fbbcf1d6954afb84584a7a7eb7ee17047748dd021519e2db5c59`. It imports the Skills assembler and assumes the private root/Skills layout, root manifests/locks and pinned local toolchain. It is not a standalone Skills test and must not be copied into the public package or this Skills commit. Its copied coin Provider descriptor/instructions/manifest hashes match the accepted synthetic payload; its expense fixture instructions are clean baseline content. Root package/lock and harness HEAD/index/worktree identities are bound separately in the manifest.

The shared Lark adapter retains its dynamic `@live-agency-skills/lark-base-client` boundary. Its private implementation, transports, profiles and authenticated execution remain separately owned and excluded. Importing selected neutral source is not a claim that all v2 operations execute in a standalone Skills checkout. No private dependency is added to the three artifacts or the isolated test graph. The historical **1,010 tests** remain accepted cross-owner qualification evidence; only the bounded four-file source test closure is proposed for any separately authorized standalone rerun. Do not add all v2 consumers or their test trees to recreate 1,010.

Receipt SHA-256 remains `512c7b4dd6bcca269e8cc2850861ed755db143bf38c89347198e877ee4c1698f`; snapshot inventory SHA-256 remains `f2ac766d43b676bfe2db30e8ac9c9e49b6b9b41b8a2480b34f292cd5ff8e27ec`. Source-to-artifact lineage is explicitly recorded for all 20 package destinations derived from 18 unique inputs. Existing tarball identities and qualification results are inherited unchanged from the accepted receipt; archives were not repacked or downloaded here. Source equality does not establish future tarball reproducibility or registry equality.

## Exact next isolated commit action

After owner acceptance of this manifest digest and 77-path patch digest, execute one local source-commit package:

1. Freshly verify the two active identities, the 82 selected source hashes/modes, the 14 baseline inputs, the private root harness binding and proposal digests. If a selected path drifted, stop that selection and rebind its actual delta; do not overwrite concurrent work or repeat a broad inventory. The active source and index remain untouched.
2. Allocate a new, explicitly approved isolated development checkout from Skills commit `e78ec0962184fee2b07ea85b3f24545438f232d4`, without dependency installation, hooks, submodule recursion or operational links. Create only the proposed isolated branch there. A separate checkout/index is necessary; do not switch or stage the active detached Skills checkout.
3. Check then apply the exact `skills-source.patch` in that isolated checkout. Stage only the 77 entries in `changed-paths.json`, preserving their recorded modes. Reject extra staged entries or missing files. Never use `git add -A`, infer membership from the implementation `new` flags, or copy an entire dirty tree. The five clean payload inputs and 14 baseline prerequisites must still match their bound HEAD blobs.
4. Compare every staged result with the manifest's expected worktree blob/mode, confirm the exact 77-path diff from the base, and confirm unchanged files remain at the baseline. Create one owner-approved commit using explicitly selected local identity and hooks disabled; no ambient identity/configuration assumption supplies release authority. Record the actual commit/tree/blob IDs after creation and link them to this manifest and accepted payload lineage in private evidence.
5. Finish with read-only source-to-payload/hash reconciliation and active HEAD/index preservation checks. A commit can be verified without installing, testing, packing, publishing or touching the private root submodule pointer. A future offline reconstruction/pack or authenticated preflight is separate scope, not an automatic continuation of commit creation.

This package defines the concrete envelope for acceptance; it does not grant commit authority itself. Publication eligibility, SEP-2, actual account/package rights, runner/workflow qualification and digest-bound publish approval remain the separate gates named in readiness. No final commit ID, full runtime freeze, clean-commit test pass or reproducible packing is claimed.

## Exclusions, verification and rollback

Exclude all other active dirty/untracked paths, root reports/locks/harness, Provider/MCP repositories, submodule pointer updates, node_modules, generated snapshots/staging/tarballs, source preimages, operational data, credentials, config/profiles, schedules, workflows, global registration, installed Skills and all `sources/` references. The two synthetic fixture tarballs are qualification evidence only; neither is a fourth release candidate. The Skills commit itself includes other accepted neutral consumers, but the artifact allowlist remains exactly the three accepted payload maps. Repository/history publication review remains necessary before any remote source distribution.

Verification performed: 67 accepted Skills hashes; exact 67/18/75 union and 82/77 final envelope; current HEAD/index/entry modes; seven prerequisite identities and 14 baseline inputs; 18-source and 20-destination lineage; assembler/map/harness identities; synthetic Provider payload hashes; narrow source credential-pattern checks and review of added prerequisite content; full patch hunk application in memory with exact reconstructed bytes; and `git apply --cached --check` against the unchanged Skills index. That Git command checks applicability only and does not stage. It passed without warnings. No embedded real data or credentials were identified in the selected scope; this is not history or legal clearance. No tests, assembler, install, pack, network, auth or source-mutating Git operation ran.

Rollback for this completed package is removal of only this new report and the three proposal files after checking their final hashes; remove the generation directory only if empty. Do not run the earlier implementation rollback script: that would undo accepted candidate source and links outside this task. Future isolated-commit rollback is abandonment of only the newly allocated checkout/branch after verifying it contains no later owner work; preserve the original active tree, staged distinctions, journals and all accepted receipts. No publication deletion or operational rollback is implied.
