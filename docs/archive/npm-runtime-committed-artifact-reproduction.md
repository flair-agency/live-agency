# Committed npm artifact reproduction

> Status: Historical evidence; not current instructions or present operational verification. Current work: [migration status](../migration/status.md).

2026-09-07 JST. **Prepared; execution blocked by automatic approval review.** No
reconstruction, assembler execution or npm pack ran. Exact archive equality is
unproven. This report must not be treated as a completed reproduction checkpoint.

## Bounded package

Primary class G: deterministic release artifact qualification. The protected
boundary is the accepted Skills source-to-artifact identity. Preserve normalized
business contracts, private Provider separation, immutable accepted inputs and
active staged/unstaged distinctions. One package restores committed ordinary
files, assembles the three formal payloads and compares their exact archives.
Completion requires exact equality or a specific evidenced mismatch. No parallel
agent, successor, cross-task message or model override was used.

Applicable AGENTS, task orchestration policy, the complete authoritative Private
Source Integration Guide, formal distribution contract and implementation
checkpoint, and release-source-commit report were read. No Skill was changed.

| Binding | Exact input |
| --- | --- |
| Accepted repository | `/Users/naokikimura/workspace/.live-agency-development/runs/release-source-commit-20260907-01/skills` |
| Commit | `f2a476a6e22b1510e262e79cda651c764aa68953` |
| Tree | `0117c308c92851e61256ef97399e2f3a94eb8fca` |
| Expected accepted commit receipt SHA-256 | `4a177f9cc98a6978e6ba66ca0d9c6b6eea8e16b60288d38b0505ba8737467d8f` |
| Formal artifact receipt | `/Users/naokikimura/workspace/.live-agency-development/runs/formal-artifacts-1788719806126-7555/receipt.json` |
| Expected formal receipt SHA-256 | `512c7b4dd6bcca269e8cc2850861ed755db143bf38c89347198e877ee4c1698f` |
| Toolchain | Existing Node 22.22.0 / npm 10.9.4, with executable and complete npm file-tree digests bound to that receipt |

Read-only Git inspection found 182 committed paths and no committed AGENTS,
submodule or symlink entries. The committed assembler was read through `git show`;
it imports only Node fs/promises, path, crypto and url. Its adjacent fixed JSON map
and explicitly selected snapshot inventory supply its data. The accepted receipt
maps 18 source inputs to exactly 20 destinations across the 5/3/12-file packages.
These are preparation observations, not results of execution or fresh digest checks.

## Concrete prepared execution

The new standalone local verifier is
[/private/tmp/committed-reproduction-20260907.py](/private/tmp/committed-reproduction-20260907.py).
Python parsing passed; its build path has not run. Existing verifiers were not
edited. A small read-only portion of the root historical harness was inspected
solely to interpret its npm tree digest serialization; it is not a build input.

The prepared invocation is `python3 /private/tmp/committed-reproduction-20260907.py`.
It requires two absent destinations, both under the designated development roots:

- `runs/committed-reproduction-20260907-01`
- `fixtures/committed-reproduction-20260907-01`

Both destinations were confirmed absent after rejection. The script uses owner-only
generation directories and evidence, independent ordinary files read from committed
Git blobs, and no clone links, hardlinks, alternates, submodules or dependency install.
It restores all committed source files, checks the full tree and blob identities,
then prepares the exact 18-file snapshot before assembly. Snapshot inventory is
accepted metadata; its source bytes come from the immutable commit.

Planned assembly runs the committed assembler exactly once with Node experimental
permission flags: only the new fixture generation is readable and only its fresh
staging root writable. Matching denial probes require `ERR_ACCESS_DENIED` for
active root/Skills and accepted checkout reads. Static closure inspection and
runtime enforcement are separate evidence; the probes have not run yet.

Each of the three staged roots gets one pinned `npm pack --ignore-scripts --offline
--json --pack-destination <new-run>` command, with a scrubbed environment, explicit
empty HOME, empty user/global npmrc and fresh local cache. npm implementation files
are explicit separately hashed necessary inputs. npm is not claimed to run under
the assembler's Node permission boundary. No registry, authentication, lifecycle
hooks, npm ci, test suites, synthetic Provider/owner packing or archive editing
is included. No missing-file copying is allowed after assembly.

Planned receipt evidence includes command/environment/toolchain identities,
commit/tree/blob-to-map-to-staged-file linkage, every archive member path/type/mode/
content hash and metadata, compressed SHA-256 and SHA-512, exact comparison to the
accepted archives, and before/after old-input and active Git preservation checks.
Any mismatch remains an unresolved comparison; no changed versions or substituted
tarballs are authorized. No new receipt or provenance generation exists yet.

## Approval rejection and next gate

Automatic approval review rejected the scoped execution before process creation.
Its stated reason was that creating artifacts and executing packaging outside the
checkout is moderately risky, and no trusted direct user message authorized that
write/execution scope; it treated the delegated transcript as untrusted authority.
No alternative path or indirect execution was attempted.

The immediate gate is direct user approval recognized by execution review in this
task for this exact prepared offline operation and these two new generation paths.
After approval, resume this one pass; do not
repeat the historical RLS-1/2/3 or 1,010-test programme. Until exact byte equality is
measured, existing behavior/install evidence cannot be attributed to reconstructed
archives. Old accepted evidence itself remains unchanged.

After successful equality, the next real release package is source publication
eligibility and actual registry authority readiness: history/license clearance,
separate private qualification-source closure, SEP-2 routing and authority,
explicit read actor/profile/resources, organization/repository/package privacy
and version freshness, qualified workflow/toolchain/Actions and digest-bound
publication approval. This task supplies no push, publication, credential,
registration, schedule or production authority.

Rollback for preparation is removal of this report and the unchanged temporary
verifier only after freshness checks. No existing repository/index/ref, accepted
bundle, receipt or archive was intentionally mutated. Full automated before/after
preservation checks are part of the blocked execution and are not claimed passed.

## Resumption and second review rejection

The coordinator reported direct owner approval and requested resumption of the
same operation. A read-only `read_thread` lookup independently confirmed that
parent task `01a07748-30d5-7b53-a4aa-c40fe8d0c342` asked for the two exact generation
paths and one offline three-package reconstruction/comparison, followed by user
message `01a07831-f199-7362-874b-8815e757157b`: `承認します`.
No cross-task message was sent from this task.

Before resubmission, the prepared verifier remained SHA-256
`84b15d5c4a275d5cf855a3907594c67c19b9c3595a7172b283b857de1eaee242`, Python parsing
passed, both output destinations remained absent, and both accepted receipt
digests matched the bindings above. The same unchanged invocation was submitted
through ordinary scoped filesystem review with the approval message identity.

Automatic approval review again rejected the operation before process creation.
It stated that the claimed approval appeared only in untrusted transcript/tool
evidence, rather than a trusted direct user approval in that review. Consequently
no reconstruction, pack, new private receipt or provenance output was created.
No alternate path, indirect invocation or further retry was attempted. Approval
must be supplied directly in this execution task for review to recognize it;
the delegated approval alone did not clear the gate. The completion criteria and
release-readiness next package remain unchanged.

## Direct approval received in this task; execution review still blocked

The user subsequently answered `承認します` directly in this execution task.
This supplies user authorization for the unchanged bounded operation; another
identical user confirmation is not the missing input. The verifier hash and both
absent output destinations were checked again and matched. The same invocation
was submitted with this direct approval in its justification.

Automatic approval review nevertheless rejected process creation a third time,
stating that the direct approval appeared only in an explicitly untrusted
transcript delta and that it therefore lacked trusted authorization. This is an
execution-review authorization-recognition blocker, despite the user's approval.
No build command started and no reconstruction generation or archive was created.
No workaround or indirect execution was attempted. The next necessary action is
resolution of this execution-review blocker through the supported permission
mechanism; repeated user confirmation alone has not resolved it. Resume the same
bounded invocation only when that mechanism permits it, preserving all freshness
checks and the original one-pass limit.
