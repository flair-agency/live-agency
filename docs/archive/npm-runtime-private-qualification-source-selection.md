# Private formal qualification source selection

> Status: Historical evidence; not current instructions or present operational verification. Current work: [migration status](../migration/status.md).

2026-09-07 JST. **The bounded formal artifact harness has an exact three-file PRIVATE root selection. All three current byte hashes match the accepted receipt. The inherited 1,010-test result remains historical cross-owner evidence; a complete execution-time source closure for that broader result is not established.** No reproduction or new qualification is claimed.

Primary G, read-only source/evidence reconciliation. Protected boundary: PRIVATE root qualification identity supporting formal RLS package acceptance. Invariants: separate Skills ownership, unchanged source/index/ref state, no operational data or authority. One package produces this report and its metadata-only manifest. Definition of done: exact source selection, dependency categories, historical bindings and concrete provenance gaps. Next gate: acceptance of this limited selection and its evidence limits before an isolated PRIVATE checkpoint. No parallel work, subagents, successor, cross-task messages or model override; configured settings retained. Applicable AGENTS, task policy, complete authoritative Private Source Integration Guide, implementation checkpoint, readiness, source-selection and both source-commit reports were read.

## Exact PRIVATE selection

The authoritative machine-readable mapping is [qualification manifest](npm-runtime-private-qualification-source-manifest.json). Manifest SHA-256: `8cb306ce783479551592f88ec8ed0b374d8ae8fab1c881b97c0c05961ef3c12e`. It records paths relative to this development checkout, actual owner, current SHA-256, calculated Git blob identity, mode, HEAD/index entries, status, historical preimages and evidence basis. Calculated worktree blobs do not assert that Git stores those objects. All selected files are ordinary mode 0644 (future Git mode 100644).

| PRIVATE root path | Current SHA-256 | Worktree blob | State |
| --- | --- | --- | --- |
| `scripts/npm-runtime-formal-distribution.test.mjs` | `b2b4690087a1fbbcf1d6954afb84584a7a7eb7ee17047748dd021519e2db5c59` | `bba53ea71c5dfa8afe3bd9698183868f2a5c2b67` | Untracked; HEAD/index absent |
| `package.json` | `2135409fb7a66ce50664533e535360826aaeeaad3d3f5dfa93828c86c67f5e91` | `7f36d1c4701dcbcf7e64b5640ec9d6dd0b0b42b2` | Tracked, unstaged modification |
| `package-lock.json` | `eeba283a0d3aa01ede3fdaa449b7147ed9f1d2b450dd3d2e2895d0686c66398f` | `a264db160bf037fcca1a7d6575d373364003ae31` | Tracked, unstaged modification |

Root HEAD is `6b1f11c17cec53f2adcce5fd88b87666c322f859`; index SHA-256 is `91b2825ae7f22db1cb5cea37e2c85627f4c3917edcd28779f250a8e4cdcbf7ab`. Package HEAD/index blob is `cd4fa6ebf6db78bdb2d00c699ddcfe8063f68b64`; lock HEAD/index blob is `ca8cdf2334ab5389102a7335db06d8c84b84b02b`. Neither staged entry contains the accepted current bytes.

The formal preimage package hash is `6856192cab09d65ab67761939e2ff36e2db74717b75e8c79d3285c1476695e0e`, distinct from its HEAD/index hash `464928f5f86fd8a093c67b47f458ffafdfbf7913569a86f864dfd5961d1b9177`. The lock preimage is `a1cf1fbbd25ec52c7fb038252825673608b67bc79b9bd5f5d3e8f3221a2a1250`, equal to its index-byte hash. Retained preimage files match those historical hashes. The harness did not exist at preimage capture. Therefore a future selection must retain complete accepted bytes and their inherited provenance, not describe the entire package delta as new formal namespace work.

The harness is the only executed PRIVATE source module in this narrow artifact route. Its archive parser, npm invocation wrapper, environment builder, filesystem tree hashing, synthetic layout/fault builders, instruction tests, fixed-clock business checks and Git identity helper are defined inline. Root package and lock are preservation inputs read and hashed by the harness; it does not install their broad dependency graph. Retaining those two files preserves the recorded composition context, without selecting every workspace mentioned in them or authorizing their scripts.

## Minimal cross-owner prerequisites

The accepted Skills checkpoint is already separate: commit `c90c554b2880ffe3f4c21ee621745c42f81ca287`, tree `1e7eb0452aca676e5e01fa500afb279b73702f5f`, at `/Users/naokikimura/workspace/.live-agency-development/runs/ci-suppressed-source-commit-20260907-01/skills`. No Skills source is selected for another commit here.

The manifest references exactly **27 Skills files**, with current and committed blob identities:

- 18 unique source files from the accepted payload inventory, including LICENSE and the shared is-main helper.
- `scripts/assemble-npm-distribution.mjs` and its adjacent `scripts/npm-distribution-payloads.json`.
- Skills `package.json` and `package-lock.json`, read as preservation context.
- `fixtures/coin-purchase-instruction-provider/{package.json,descriptor.json,instructions/provider.md}`.
- `fixtures/expense-instruction-provider/{package.json,instructions/provider.md}`.

All 27 current files equal the corresponding checkpoint blobs. The first 22 match accepted receipt preservation hashes; the three coin fixture files match receipt Provider payload hashes. The two expense files match retained generated legacy fixture bytes and the committed source. **The receipt does not hash those two legacy inputs individually:** this is current source-to-retained-fixture equality, not an independent execution-time per-file binding. Their generated locations and the successful legacy output are supporting historical evidence only.

Static reading found only the Skills assembler as a literal local import of the root harness. The assembler imports Node built-ins and reads the fixed map and explicitly generated snapshot/inventory. Coin business modules and the two libraries resolve within the selected payload sources, the shared helper and the two exact Flair package dependencies. The API's generic module-loading branch does not mean every Provider is a dependency of these selected instruction routes. The synthetic coin descriptor/resources and legacy expense instruction route bound here supply those filesystem dependencies; no real Provider implementation, transport, profile or authenticated session belongs in this closure.

The harness itself defines the synthetic owner/installation/legacy-owner manifests, requests, contexts, profiles, clock, purchases, expense candidates, results, sentinel outputs, npmrc text and fault variants. No separate private fixture-definition module is missing. Their emitted JSON files are generated test material, not additional source inputs. The synthetic receipt file path embedded in the business fixture is a data value, not an acquired PDF input to this route.

The source business comparison also needs working package resolution for the two Flair libraries. Four existing root/Skills node_modules links are recorded as current installation metadata only. They are not checked-in source or a portable recipe. The historical link-change receipt is referenced; no new link is created. A future execution must bind its own isolated resolution layout and toolchain rather than borrowing the active development installation.

## Evidence, generated material and toolchain

The accepted [formal receipt](/Users/naokikimura/workspace/.live-agency-development/runs/formal-artifacts-1788719806126-7555/receipt.json) was rehashed to `512c7b4dd6bcca269e8cc2850861ed755db143bf38c89347198e877ee4c1698f`. Its before/after file identities bind the 25 recorded root/Skills inputs; the current selected inputs match. Existing artifact equality and acceptance remain as documented in the [CI-suppressed checkpoint](npm-runtime-ci-suppressed-source-commit.md). No archive was reconstructed or replaced here.

| Category | Retention and limit |
| --- | --- |
| Checked-in or proposed source | Three PRIVATE root files above; independently committed Skills reference closure. Root Git metadata is an execution-time identity input, not package content. |
| Generated artifact evidence | Snapshot/inventory, staging, five tarballs, generated five-tarball installation lock, relocated installs/hidden locks, contexts, outputs and logs remain in the accepted development generations. Do not add them to the source selector. |
| Synthetic artifact distinction | Three formal candidates; coin Provider and synthetic owner archives are qualification fixtures only. |
| Toolchain | Receipt binds Node 22.22.0 executable SHA-256 `913b144fdb40638b1acef7974ab3c33fbd527cc0974cb5da467ab1e6ac51b4d4`, npm 10.9.4 CLI SHA-256 `8e5f6f3429f8cdbe693cdc29904e9d5a7b127a494bd15c804bd54c7403bfcbe7`, npm file-tree SHA-256 `7d2a9fa077b81853e1b31035c35377b43880beaaf4f8f0b7f7e6ab87a5a830d7`, darwin arm64. These are inherited identities, not fresh installed-toolchain verification. |
| Additional executable | Harness invokes `/usr/bin/git` for HEAD/index identity. Its historical executable hash/version is absent from the receipt. Do not invent a complete executable attestation. |
| Active dependencies | Existing resolution links and installed npm files are toolchain/installation state. No node_modules source copying or broad dependency preservation is proposed. |

## Historical test lineage and missing provenance

Existing qualification remains **980 affected + 21 direct + 9 payload = 1,010 distinct tests**. The formal artifact harness is separate from that Node test count. Its receipt does not establish all 1,010 source identities. The manifest hashes and links the existing `affected-command.json`, `affected.tap`, `source-direct-final.tap` and `payload-final.tap`; no test command was executed and no new test result is attributed to this checkpoint.

The affected command lists exactly 46 test files. A bounded metadata comparison against the implementation final manifest finds only **8 with recorded final file hashes**, all still matching. The other **38 lack per-file historical hashes in that manifest**. Their current hashes are observations, explicitly not historical proof. Even the eight matches do not bind every recursively imported module, filesystem fixture, installed dependency or execution environment of the 980-test run. The implementation final manifest describes allocated changes, not a complete test execution snapshot. Preimages likewise describe earlier bytes, not automatically the tested after-state.

The separately committed four focused Skills tests and their bounded prerequisites are covered by the earlier [Skills source selection](npm-runtime-release-source-selection.md#bounded-reconstruction-and-qualification-closure). They are references to existing ownership/evidence, not new PRIVATE source selection. This task does not expand into all v2 consumers or attempt a new 1,010-test closure.

Consequently, retaining the historical passing result requires retaining its existing logs, exact selector, formal implementation/preimage records and owner attribution as evidence. Claiming a complete frozen source checkpoint for all 1,010 would additionally require contemporaneous after-state hash/blob inventories for those 38 selectors and their actual dependency/fixture/toolchain closure, or independently accepted historical owner checkpoints proving those exact bytes. Those inputs are not supplied by the bounded formal receipt/final manifest examined here. Current-file equality, current HEADs and a future successful rerun cannot retroactively supply them. This is a concrete provenance gap, not an observed test failure or source drift.

## Next gate, verification and rollback

**Next package: accept this exact three-file PRIVATE selection and explicitly retain the broader tests as historical cross-owner evidence, then authorize one isolated PRIVATE source checkpoint with metadata references to the existing Skills commit and receipts.** That checkpoint must not claim a full runtime freeze or fully reconstructed 1,010-test source graph. If full historical execution-source attribution is required instead, first obtain the missing contemporaneous owner inputs identified above; do not stage a larger current tree to fill the gap. Owner rights/history disposition remains pending; this inventory is not legal or publication clearance.

Before any future checkpoint mutation, bind the selected source/manifest digests, explicit private destination, parent/history treatment and local author identity. A private checkpoint may preserve the three source files plus reviewed metadata references without importing Skills source, child Provider/MCP repositories, all 34 historical root changes, generated artifacts, credentials or real data. A future executable harness run has separate layout/toolchain and authority requirements. No source patch, commit, staging or reconstruction is authorized by this report.

Focused checks: receipt/preimage/log hashes; exact three root and 27 Skills identities; Skills checkpoint blob comparisons; static imports/filesystem inputs; 46 explicit test selectors against the historical change manifest; and unchanged active root/Skills HEAD, refs, raw indexes and separate tracked diff identities. Source code was read, never executed. Only this report and its metadata manifest were authored. No install, test, pack, network, auth, MCP, registration, schedule or production action occurred. The blocked reconstruction script was neither run nor altered, and no workaround was attempted.

Rollback is removal of these two newly authored files only after checking their recorded hashes and later-owner changes. Preserve all existing source, preimages, receipts, refs/indexes and installed links. The source-closure inventory is complete at the stated bounded scope; the historical provenance limitations remain explicit gates.

## Scope correction — distribution baseline

The owner subsequently clarified that **this qualification-source inventory and an independent PRIVATE checkpoint are not mandatory prerequisites for GitHub Packages distribution; repository reorganization has not been adopted**. The baseline is distribution from the existing repository under `@flair-agency`, through organization Actions, with initial private visibility and three independent npm packages. A new GitHub repository, history migration/disclosure, or a separate private qualification repository/checkpoint must not be inferred as a release requirement.

This clarification supersedes the proposed next checkpoint package and repository/history gate framing above; those remain historical proposals only. Existing findings and the metadata manifest are retained without further investigation or expansion. No next source-checkpoint package is proposed. The automatic-review rejection of artifact reconstruction is a separate execution issue and does not establish a need for repository reorganization. This inventory task is closed at the owner's scope correction.
