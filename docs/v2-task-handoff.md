# LIVE Agency Runtime v2: Current dispatch entry

## Development resumption — offline authority only

2026-09-06: the owner authorizes offline v2 implementation in the independent
`live-agency` Codex project at `/Users/naokikimura/workspace/live-agency-provider-runtime`.
SEP-1 source separation and local execution are accepted. SEP-2 host isolation
and actual integration authority remain unresolved; globally visible business
Skills do not grant permission to execute them. External IO, connectors, native
execution, ambient credentials, activation and schedules remain disabled here.
Production recovery and its six active schedules belong to the separate recovery
coordinator; this task does not change them or its authoritative ledger.
The historical technical results below remain evidence. This notice supersedes
the stale SEP-0-first queue only for owner-authorized offline development.

## 4. Next work package

**2026-09-07 LIVE standalone selected executor implemented offline; accepted by coordinator.** The coordinator-resolved historical admission
exception is implemented as a private purpose-bound handle: complete selected
stored bytes and original expectations, then current restore inspection/rebind.
Upload/delete retain exact original-plan freshness. Standalone delete/restore
use independent exact rebind/mutation approvals, immutable ordered batches,
durable target/attempt consumption and read-only whole-effect reconciliation.
[Conformance, limits, commands and preservation](../provider-runtime/docs/v2-live-history-compaction-execution-conformance.md):
**217 distinct passing tests** (72 new executor/Provider/CLI +145 retained).
Original blocker characterizations now prove success and cross-purpose rejection.
No live authority or CP1 claim. Coordinated full-backup/parent executor and media
holds remain.

**2026-09-07 selected avatar media contract decision completed offline; proposed
for coordinator acceptance.** [Concrete contract and one implementation brief](../provider-runtime/docs/v2-selected-avatar-media-contract.md)
separate supported User/Tenant stored-byte reads and profile upload-before-create
from independently unverified `attachments:append`. The additive private row-media
adapter binds original/stored bytes to creator/field/resource, exact lifecycle
and immutable multipart intent, with durable upload/create consumption and full
same-actor readback. Core multipart already exists; the JSON-only Skill port and
per-row evidence admission are the actual gap. Original source metadata alone
is not provenance. Unknown upload without a token stays unresolved; no blind
replay or invented lookup. Invitation read/comparison/archive preparation can
advance independently; append-dependent sync/resume/restore mutations remain held.
Invitation historical/execution and coordinated maintenance remain independent.
Only the contract document and this §4 entry changed; static verification only,
no runtime/live effects or CP1 claim. Task `01a07854-5e7d-77e2-baab-9f7e15c13d59`
used host defaults without agents or successor dispatch.
**Next one package, after coordinator acceptance:** implement selected verified
avatar bytes and profile upload-before-create with direct invitation preparation
hydration wiring and synthetic tests, exactly as scoped in the contract brief.
Separate scoped authoritative append evidence retrieval remains required before
append support can be promoted; no external retrieval is authorized here.

2026-09-07: **Invitation compaction selected non-avatar preparation completed
offline; family conformance remains partial.**
[Conformance/commands](../provider-runtime/docs/v2-invitation-compaction-caller-conformance.md),
[21-path inventory](../provider-runtime/docs/v2-invitation-compaction-caller-inventory.json).
Direct **68/68** and affected shared factory/export callers **440/440**:
**508 distinct passing tests, no overlap**. No external operations occurred.

Selected User/Tenant plan, stale-plan inspection, local self-contained gzip and
restore conflict inspection bind explicit actor/profile, exact target, full rows,
fresh schema and standalone/coordinated workflow. Named v1 rollback is retained;
no ambient fallback or mixed artifacts. Adjacent equal runs keep newest and
A-B-A remains three runs. Exact text now preserves nickname Unicode/whitespace
and external-ID whitespace; malformed text/boolean timestamps block. Legacy
avatar byte backup/archive/restore remains tested. Any selected stored avatar
holds the entire plan/restore before byte IO; metadata cannot replace bytes.

**Required gaps remain:** the actual selected attachment inventory reader has a
separate evidence contract, not a per-row avatar handoff. Selected multipart
upload binding and independently unverified append are open. Profile new-avatar
upload-before-create does not inherently require append; missing-avatar resume
does. Required profile/invitation media remains on the mandatory queue.

Offline selected row storage and cross-invocation receipt verification are now
implemented through a separate private adapter/composition and neutral validators.
The full-Base product remains unchanged. Programmatic LIVE/invitation receipt
preparation requires an owning opaque handle; persisted JSON is revalidated from
complete stored bytes. Invitation selected deletion/restore and CLI manual receipt remain
held; LIVE standalone execution is accepted as described above. Source bytes and read-only preparation bindings remain unchanged.

**Coordinated maintenance remains held:** owning planner lacks execution handoff.
LIVE/invitation require BOTH verified row archives and full same-Base/schema
content-verified backup no earlier than the child plan, exact parent/child
SHA/count approval, serial stop and post-backup remaining-count handoff. Do not
relabel standalone. Profile/metric participants retain their different contract;
no new row archive/restore requirement is imposed on them.

Inherited LIVE compaction **84/84 +380/380 (464 distinct)** remains accepted
[checkpoint](../provider-runtime/docs/v2-live-history-compaction-caller-conformance.md); its selected suite
passes as an affected caller here. Metric **120/120 +287/287 (407 distinct)** and
profile compaction **111/111 +202/202 (313 distinct)** remain accepted. Required
profile/invitation sync media remains partial; previous non-avatar and completed
core/backup/gift/activity evidence is retained. Optional recovery improvements
supply no new migration gate.

**2026-09-07 row-storage implementation accepted offline by coordinator.**
The coordinator accepted the [complete design](../provider-runtime/docs/v2-row-archive-storage-contract.md)
before implementation; this supplied no owner approval for live connection.
[Conformance, commands and preservation](../provider-runtime/docs/v2-row-archive-storage-conformance.md):
**253 distinct passing tests** (50 Drive, 39 neutral, 38 composition, 61 LIVE
selected, 51 invitation selected, 7+7 existing pure/legacy). Actual file-journal
and separate Node-process rehydration tests pass. Full-Base code is preserved.
Durable dispatch and approval consumption precede create; unknown outcomes never
reupload. A unique discovered candidate remains origin unknown until separately
approved for adoption. Forged persisted JSON/actors/handles do not grant authority.
Inventories now cover 23 LIVE and 25 invitation paths.

Concrete selected authenticated Drive client/profile/destination and external
authority after SEP-2 remain required. Invitation/profile avatar acquisition,
multipart/append, exact-plan Lark delete/restore execution with reviewed rebind,
and coordinated full-Base plus parent/child maintenance executor remain mandatory
unfinished work. No source/media acquisition or live recoverability is claimed;
**CP1 is not complete**.

**Current package:** LIVE-history standalone selected delete/restore executor,
using accepted row verifier and explicit preparation-to-mutation rebind. Task
`01a07834-291c-7642-bfac-2d855ecf7223` owns this implementation and next handoff.
Formal npm candidate migration is accepted: 115 current hashes verified and
1,010 distinct tests; use the new `@flair-agency` API/files identities while
retaining lark-base-client. No publication or external mutation is authorized.
Coordinated executor and required invitation/profile media remain separate holds.

Row-storage preimages, exact final hashes and preservation evidence:
`/tmp/row-archive-storage-preimages-20260907`.
Synthetic journal runs: `.live-agency-development/runs/row-archive-storage-20260907`.

Coordinator: `01a0772d-7eaa-7e23-86a8-73d646470bbf`.
Preservation/preimages/final hashes: `/tmp/invitation-compaction-before-20260907`.
Synthetic assets/logs: `.live-agency-development/{config,fixtures,runs}/invitation-compaction-20260907`.
Root/Skills/Base staged diffs and inherited dirty state preserved; accepted RLS-1
reserved-source changes were not edited. No staging, commits, pins/dependencies,
worktrees, production/auth/external IO, CLI/browser/MCP execution, schedules,
global registrations or host changes. Host defaults retained. SEP-2, W2,
App/OAuth/scope, activation and scheduled-cycle gates remain outside scope.

## 以前のbackup checkpoint — 現行キューではない

以下は実績と当時の停止条件を保存した履歴。撤回されたhost保証・添付条件、旧next-actionは上記の分類に置き換わる。最新roundtripより前のscope-blockerを現在の停止理由として再利用しない。

2026-09-06 owner decision: record the restorable Lark Base backup capability as a
[post-migration Provider improvement](v2-migration-plan.md#7-post-migration-improvements).
Future Provider feedback/implementation is outside v2 completion and activation
requirements; this documentation update initiates no implementation or live test.

**Latest: bounded CLI roundtrip complete.** [Result and assets](../provider-runtime/docs/backup-roundtrip-result.md): fixture-only PASS with attachment supplement; native .base alone omits attachments. Two Bases retained; no further roundtrip work remains. Prior scope-blocker notes below are historical. Production recovery, backup gate fixes and activation remain separate and unchanged.

Latest roundtrip: second OAuth (8 scopes) completed; first-table readback succeeds, 0 records. Table constructor now reports two further required User scopes: base:field:update and base:view:write_only. No second table created; reuse existing source. See latest roundtrip checkpoint.

Roundtrip follow-up: OAuth 10 scopes granted; one Flair synthetic source Base created. Eight additional CLI User scopes identified; see latest section of [roundtrip checkpoint](../provider-runtime/docs/backup-roundtrip-checkpoint.md). Reuse recorded source; no export/import or attachment verification yet.

2026-09-06 bounded CLI roundtrip follow-up: [observed scope blockers and prepared fixture](../provider-runtime/docs/backup-roundtrip-checkpoint.md). Both import and Base-create dry-runs report missing user scopes; zero external writes/assets and no restoration claim. No permission expansion performed. This separate fixture experiment does not alter the inherited backup/activation gates below.

2026-09-06: **Public-schema host review complete; E2E host still unsupported.**
[Capability matrix and decision](../provider-runtime/docs/backup-live-smoke-preflight-checkpoint.md#host-capability-resolution--2026-09-06).
Drive `fetch(download_raw_file:true)` explicitly supports complete raw content,
with streamed file reference or legacy base64. Missing host guarantees remain:
operation-bound actual actor, revision-consistent raw materialization, exhaustive
binary-folder pagination, and browser correlated complete download/session
continuity. Cached download `path` is undocumented and was excluded. No live
profile/session/private-data reads or external IO; auth-free CLI help/schema only.

Current CLI 1.0.93 has documented `.base` export, ticket/artifact correlation and
local download. Attachment-option completeness and actor binding remain unproved;
this is an alternative-route review candidate, not an activated replacement for
the approved browser procedure. No endpoint or hidden API inferred.

Next one outcome: runtime maintainer supplies concrete supported host methods and
authentication/refresh/retry/revision contracts identified in the checkpoint.
No further unchanged preflight, abstract adapter package or scope request now.
The two missing import scopes (`docs:document.media:upload`, `docs:document:import`)
are independent and cannot fix host gaps. Resolve host first, then obtain only
still-needed consent and perform bounded integration plus the existing approved
single synthetic E2E. Session guarantees and all recovery/activation/attachment
gates remain unchanged; immutable verified API credentials are a documented
clarification proposal, not a caller-boolean waiver.

Inherited My Drive single-folder compatibility remains complete: null sharedDriveId,
identical exact folders; **96/96** and syntax/diff success inherited, not rerun.
Existing ignored fixture/preflight and dirty work preserved. This review changed
only checkpoint and handoff; no source, auth, profile or external asset changes.

### Inherited adapter implementation

2026-09-06: **Full-Base Provider adapters and local composition implemented.**
[Contract Sections 6–7](../provider-runtime/docs/v2-full-base-backup-caller-contract.md#6-minimal-provider-io-decision--2026-09-06)
now have both adapter implementations, direct synthetic tests and the explicit
`scripts/full-base-backup-adapters.mjs` factory. Stored receipts carry the inline
owner-only counterpart; existing normalization excludes it from public results.
Focused tests: **39 new + 46 runner regression = 85/85**. See the
[implementation checkpoint](instruction-audit-ia0-ia2-checkpoint.md#full-base-provider-adapter-implementation--2026-09-06).

Inherited dependency: provide/review the two concrete trusted host handles described
in Section 7: same-session browser native export/correlated complete download and
selected Drive create/list/full raw read with unknown-write reconciliation.
Their actual clients and identity/resource/session guarantees remain unverified;
this local implementation does not connect to live services or prove authority.
No further abstract contract package is needed. Capability/source completeness,
import recovery, live/readiness/activation and independent attachment gates stay
open. Manual-export receipt inquiry stays closed. Initial dirty work and current
component registrations are retained; no production configuration changed.

Packaging update, 2026-09-05: the user confirmed that the two supplied artifacts
were manually exported from Lark. Their provenance/receipt inquiry is closed;
no further receipt search or repeated artifact inspection is needed. Observed
definitions remain valid sample evidence, while complete source coverage, import
recovery and selected Provider capability remain unproved. These samples are
not Provider execution artifacts and supply no certified Consumer input.
Any later Provider acquisition/recovery verification is separate work; this
closure does not initiate live operations or fabricate a certification.
See [evidence and limits](../provider-runtime/docs/backup-offline-artifact-evidence-checkpoint.md).

The independent attachment provenance/currentness owner gate also remains open;
see [owner decision packet](../provider-runtime/docs/v2-m2u-2b-attachment-evidence-owner-review.md). It is
not satisfied by the supplied export. W2 and other live gates below are retained.

For remaining work, apply the [grouping map](v2-work-package-consolidation.md)
and [worker brief](v2-task-execution-instructions.md#worker-brief-and-dispatch).
After a real dependency is resolved, combine the accepted logical implementation,
direct tests and necessary scoped local wiring. Use Astra/low explicitly. A
separate worker requires a stated gate/decision/dependency reason. The coordinator
may select another already-authorized independent unblocked outcome from the map;
this packaging update does not authorize evidence acquisition or live changes.
Update this entry in place after the next verified result. Reconcile any newer
worker result before dispatch; do not replay completed packages from older text.

## Earlier checkpoint excerpts — evidence, not queue instructions

All next-action statements in the excerpts below are historical. The current
entry above owns dispatch; detailed workflow gates remain applicable.

### Latest bounded package — supplied offline export evidence

2026-09-05: [limited evidence checkpoint](../provider-runtime/docs/backup-offline-artifact-evidence-checkpoint.md)
completed using only the two user-supplied artifacts. Structure/view/formula/link,
dashboard and automation definitions are present; full source coverage, import
recovery, selected Provider capabilities and authentic execution/receipt binding
remain unknown. No certified Consumer input or live operation. Existing dirty
work and attachment owner-review holds are retained; earlier 20/20 is not rerun.
Next one decision: can the owner supply an existing execution/receipt record bound
to these exact artifact hashes? Otherwise retain user-provided-export status;
do not repeat artifact inspection or invent a run. Earlier checkpoints below
are historical and do not supply that missing evidence.

### Latest bounded package — Base-only capability integration

2026-09-05: authorized local `buildDrillPreflight` integration completed.
[Checkpoint](../provider-runtime/docs/backup-consumer-integration-checkpoint.md): explicit companion input,
shared pure assessment, required records/structure, optional auxiliary avatars
only; non-met/unknown required results return no plan. Separate review preserves
optional failures and legacy plan/receipt hashes. Direct synthetic drill tests
20/20; no live operation, capability certification or setting change.
Next one package: review supplied offline Provider structure/settings and
execution/artifact/scope evidence for explicit inputs. Existing attachment
owner-review holds remain unchanged. Earlier review below is historical.

### Latest bounded package — one backup Consumer requirements review

2026-09-05: reviewed the existing Base-only `buildDrillPreflight` Consumer in
`skills/live-agency-skills/skills/lark-base-disaster-recovery-drill/scripts/drill_core.mjs`.
[Direct review checkpoint](../provider-runtime/docs/backup-consumer-requirements-review.md): recommend required
records/structure and optional reacquirable auxiliary avatars for this use only.
Provider capabilities remain uncertified; no integration, business policy, live
operation or setting change. Existing receipt/approval/readback and attachment
evidence/owner-review holds remain unchanged. Next one package: scope and authorize
the bounded synthetic Consumer integration described in the checkpoint.

### Latest bounded package — backup capability common contract

2026-09-05: approved source-neutral capability contract implementation completed;
[checkpoint](../provider-runtime/docs/backup-capability-contract-checkpoint.md), [schema](../provider-runtime/docs/backup-capability-contract-design.md#6-共通実装の入出力2026-09-05).
Direct contract/API tests: 13/13. No business adoption, live operation or capability
certification. Existing attachment evidence/owner-review gates below remain open.
Next one package: select one Consumer and review its required scopes and tolerated
limitations before a separately authorized business integration. Do not activate it.
The following earlier M2U handoff remains its independent gated queue.

### Earlier M2U handoff

- Updated: 2026-09-05, M2U-2b real-evidence owner review packet prepared locally.
- Current verified local result: attachment four-operation mocked CLI composition passed 16/16; M2U-4b/M2U-4a launchers, M2U-4e and IA-0–IA-2 remain completed.
- Next one package: **M2U-2b — owner decision on real attachment evidence provenance/currentness
(Astra/low intake; live access remains gated)**.
The private factory/channel and minimal consumer/launcher handoff are now
implemented locally under the accepted contract. The real Provider factory
composes with both completed core transports, requires lifecycle version 1,
checks exact frozen context and run-local inventory membership, and denies
missing/stale/changed/unknown evidence before authentication or dispatch.
Direct factory tests passed **61/61**, affected acquisition **16/16**, launcher
**59/59**, and mocked CLI **16/16**. These are synthetic results, not live proof.
See [Provider implementation evidence](../provider-runtime/docs/v2-m2u-2b-attachment-authorization-checkpoint.md#provider-factorychannel-implementation).
The preceding core 102/102 result is inherited; core files were preserved.

- Next one outcome: owner-reviewed identification of the producer/verifier and
  authoritative evidence sources for exact actor, effective scope alternatives,
  unfiltered Base/table ACL, attachment locations and mapping/export provenance;
  decide approved validity, generations and current revocation semantics.
- Gate: no source is identified yet. Do not substitute receipt/hash/selection
  claims, caller JSON, synthetic fixtures or absent revocation notifications.
  Unknown evidence keeps the production route unavailable. Async collection, if
  needed, is separate preparation and does not replace the synchronous gate.
- Excluded: actual CLI/auth/profile/credential/grant access, API/download, live
  evidence collection, install, restart, activation, schedule, staging or commit.
- Reasoning: Astra/low for bounded source/contract intake. Record the specific
  unresolved question and return condition before any scoped effort exception;
  no setting change is claimed and no task/subagent/worktree was created.

Owner review preparation is complete: see the
[decision packet](../provider-runtime/docs/v2-m2u-2b-attachment-evidence-owner-review.md).
Recommended next one action, only after owner supplies the exact target, existing
local evidence sources and bounded review authority/expiry/storage: one offline
source-suitability review, external request budget zero. No source/actor/Base was
selected, no approval consumed, and no live producer or currentness proof exists.
The packet separates implemented checks from missing authentic evidence and
records later acquisition bounds without authorizing them. No additional local
implementation can be specified until the source contract is known.

M2U-2b as a whole, M2U-3/5, workload performance and rollout gates remain open.
Naming, IA-3/IA-4 and gated work below remain separate.

### Existing gated W2 work

**M4 wave 2 profile-history write-profile candidate and zero-mutation
rehearsal** remains prepared and gated as follows. It must stop before activation
and make zero destination mutations. Selecting User is not an automatic waiver
of the dedicated-app or shared-Principal isolation rules.

Definition of done:

1. Reconcile the active Creator Scouting read bundle against the fresh
   authoritative export and the exact profile-history tables, fields, due view,
   and allowed API routes. Treat the project copy as reference evidence, not as
   a current authoritative source.
2. Generate one owner-only `candidate` profile and one separate candidate
   Principal for only the profile-history route. If the existing isolation rule
   permits the exact active read API app on the same Base, prove that binding by
   fingerprint without persisting or disclosing the Secret; otherwise stop.
3. Prove candidate file ownership and mode, fail-closed startup while still
   `candidate`, and a memory-only active-contract simulation. Do not modify the
   active owner-only bundle.
4. Reuse the exact 20-row observations and dry-run result from the equivalent
   comparison to prepare a content-bound profile-history write intent, then run
   a transport-enforced read-only preflight against the live destination.
5. Record the exact intent hash, create/update/attachment/already-applied
   counts, bounded request budget, and zero mutation requests in an owner-only
   receipt. Stop before profile activation, intent execution, schedule switch,
   or version 1 retirement.

Activation requires a separate explicit owner decision. Any later execution
requires a fresh unexpired intent plus explicit confirmation of its exact hash
and all mutation counts. A schedule switch remains a still-later gate and must
retain version 1 rollback until two successful scheduled version 2 cycles.

The candidate builder and its synthetic fail-closed suite are now implemented.
The first production candidate attempt stopped before output or authentication:
the active read credential is already shared by the read Principal and the
active invitation-history write Principal, while the profile-history isolation
contract permits at most one other Principal to share a credential. The legacy
profile configuration points to that same credential. Do not create an alias
for the same API app or weaken the isolation contract. Resumption requires the
exact Keychain credential reference for a distinct dedicated profile-history
API app; the builder will fingerprint it without persisting or disclosing the
Secret. No candidate file was created.

### Parallel Wave 3 handoff

The `creator-live-history-sync` migration-only adapter and comparison CLI are
ready for synthetic use. Coverage is checked from actual observations as well
as the reviewed manifest; shared missing rows, unexpected accounts, wrong
record/account associations, and blocked dry runs cannot yield `equivalent`.
Explicit unavailable rows remain visible and do not prove successful acquisition.

The MCP LIVE target row now requires `liveContext.cutoffAt` and
`liveContext.knownEvents` (exact start/end anchors). Both observe and validation
responses preserve them and bind them to the row hash. The common profile and
invitation envelope is unchanged. Do not strip this context, invent empty
anchors, or pass generic selection output directly into LIVE acquisition.

Next production steps remain separate: verify the selected running MCP supports
this envelope, obtain one reviewed Skill target export, run both authorized
acquisition paths with the same context, and compare their destination dry
runs without applying either. Runtime activation/restart was not part of the
synthetic work. Resolve any unavailable coverage before treating production
readiness as complete. LIVE/metric domain write semantics and route activation,
scheduled-route approval, and two successful scheduled cycles with v1 rollback
retained are still pending. Do not infer those authorities from a passing report.

Focused checks are the public `test/live-history-v2-dual-run.test.mjs`, composed
`scripts/v2-live-history-dual-run.test.mjs`, and the Skill's `quick_validate.py`
validation. The composed tests use synthetic providers and in-memory MCP
transport only; they exercise both module and instruction acquisition paths,
context/hash failures, validation, dry-run planning, and final comparison.

### Other gated work

The separate **M3 private Scouting write-profile review and activation** remains
gated. Do not begin it until the user supplies a distinct write credential
reference and explicitly approves activation of
`flair-creator-scouting-write`. At that point, build the owner-only profile from
a fresh authoritative export, prove its immutable Creator, assignment,
activity, field and action-option bindings, verify the dedicated API Principal,
and run a bounded dry-run/preflight with zero production mutations before
requesting any operation-specific approval. Until that gate is opened, the
implemented write process must remain absent from active MCP configuration.

Creator Networks Chat acquisition remains the read-only M4I workstream and
never uses Flair credentials or accounts in production. The Phase 3 execution
harness is ready. A Flair test app may exercise only the isolated user-OAuth
conformance candidate and one dedicated test chat; it never enters the Creator
Networks production profile or fallback chain. The owner cannot add a Bot to
the Creator Networks target and expects an addition request would likely be
rejected, so no Bot-addition request or tenant-token route is part of M4I. The
browser route can be selected explicitly while Creator Networks user scopes are
under review; after approval, the same human's user-OAuth API is the planned
primary and the browser can become a grade-`B` fallback after same-user and
protected-scope continuity is proven. Payload equality is not required. The
exact browser feasibility read, MCP candidate, reviewed production activation,
and active-runtime replay now pass. The browser identity fingerprint is not an
official Lark `open_id` and cannot establish API/browser equivalence by itself;
the API route remains absent until app approval and independent same-user
verification.

Do not implement new candidate registration, username or alias mutation,
automatic parent mutation, person matching, account merge/split, or an
account-level membership transition without its own reviewed contract and
approval.

## 5. Earlier blockers and decisions — disposition now owned by §4

This inventory preserves earlier evidence; it is not a dispatch queue. Apply §4
and migration-plan §5/§7: optional work is deferred, withdrawn gates are not
reinstated, and unresolved code restrictions remain explicitly unimplemented.

- M2U has an accepted design but no completed all-Provider User/Tenant
  implementation or rollout. The inventory/selection contract is implemented; remaining local
  work can proceed independently of production app review and W2 credentials after the completed IA-0–IA-2 checkpoint. Later live actor
  changes require fresh preflight and a valid actor-bound execution authority;
  neither generic support nor equal target hashes proves access or approval.
- M4 wave 1 restart and scheduled-cycle verification are complete at 2/2; they
  are no longer an open blocker. Keep the v1 rollback artifacts retained.
- M4 wave 2 comparison is complete and equivalent for the exact reviewed
  20-row input. Eight rows remain explicitly browser-blocked because the
  admin-enforced browser policy became unavailable. A later fresh browser-tab
  reconnect still failed the same policy check and was not retried or bypassed.
  Rerun those rows before treating acquisition coverage as complete or
  approving a schedule switch.
- The profile-history write route is implemented but still has no reviewed
  production candidate, active profile, active MCP configuration, or schedule.
  Candidate tooling is ready, but the active read credential is already shared
  by another write Principal and cannot be reused a third time. Supply an exact
  Keychain reference for a distinct dedicated API app to resume the zero-
  mutation candidate rehearsal. Activation and later intent execution remain
  separate owner approvals.
- The production Organization and API-app Principal remain only in the
  owner-only private bundle. They must not be copied into checked-in config or
  reused as evidence of write authority.
- Creator Networks Chat browser-session organization, derived human-user
  fingerprint, exact target, one bounded message scope, loading completeness,
  thread boundary, and safe read-only interaction are verified. Effective
  source-history retention remains unproven. The owner reports that they cannot
  add the Bot to the exact chat and expects an addition request would likely be
  rejected. Do not request or attempt that membership change; Bot/tenant-token
  routing is excluded. The browser-only production Profile and runtime are now
  active read-only. After app approval, the user-OAuth API
  must independently prove its exact app, official user ID, scope, and chat
  visibility; equivalence remains disabled until a same-user differential
  passes.
- Exact one-chat allowlists exist for the isolated Flair test and Creator
  Networks routes. Flair user-OAuth conformance is complete; the previous bot-
  outside-chat result is historical and no Bot addition is required. The
  Creator Networks browser runner, active Profile, and active MCP runtime pass;
  the independent private Provider remote and composition submodule are also
  complete.
- Verify the lifecycle and semantics of TikTok User ID and BackStage Creator ID,
  or continue treating their stability as unknown.
- Verify the authoritative multi-account invitation policy before automating a
  policy conclusion, and design any parent-relation write separately.
- Implement the real private attachment acquisition/package and restore-mapping
  adapters, then connect complete production inventory to the version 2
  retention planner. The manifest, receipt, drill, and retention synthetic
  contracts are complete; live attachment recoverability is not yet proven.
- Prove one end-to-end unattended backup run and finish shared-storage
  permission hardening.
- The larger restored Scouting Base returned a Lark request error on re-export;
  table counts and UI structure were verified, but a second artifact hash was
  not obtained.

These blockers do not invalidate the completed contracts, synthetic write
integration, fresh export reconciliation, active read profile, or separately
activated invitation-history write profile or the separately verified r6
operation. Neither the profile activation nor that one operation authorizes a
schedule switch.

## 6. Repository safety and verification

Existing root, Provider, MCP and public Skill dirty changes are retained and
attributed by family in the IA checkpoint. No staging or commit was performed.
The previous commit/pin and full-suite evidence remains in
[historical Section 6](v2-task-handoff-history-2026-09-05.md#6-repository-safety-and-verification);
it does not prove current dirty work is committed or remote-reproducible.
Run checks relevant to new changes; retain existing cutover and rollback gates.

## 7. References to open only when needed

- Migration milestones and exit criteria: `../docs/v2-migration-plan.md`
- Approved IA sequence and release coverage: `../docs/instruction-audit-and-simplification-plan.md`
- M4 wave 1 scheduled cutover scope and success gate:
  `docs/v2-m4-wave1-scheduled-cutover.md`
- Domain/MCP roadmap: `../docs/live-agency-mcp-roadmap.md`
- Domain knowledge: `../docs/live-agency-domain-knowledge.md`
- Accepted identity and multi-account decision: `docs/v2-identity-model-decision-gate.md`
- Current Lark mapping: `docs/v2-m2-lark-data-model-plan.md`
- Capability ownership: `docs/v2-capability-inventory.md`
