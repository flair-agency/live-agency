# Formal npm distribution implementation checkpoint

2026-09-07 JST. **Offline implementation and scoped qualification complete;
coordinator acceptance pending.** Configured host model/effort retained; no override,
subagents, successor, publication or host registration. The current accepted
[formal contract](npm-runtime-formal-distribution-contract.md) and
[distribution decision](npm-runtime-distribution-decision.md) govern this package.
The complete authoritative governance guide and task policy were read.

## Change card and ownership

Primary E, supporting D/G. Protected boundary: candidate library identity and
composition, with full coin Skill artifact assembly as accepted supporting work.
Normalized business behavior, API_VERSION 1.6.0, private Provider boundaries,
explicit authority selection and old operational composition remain preserved.
One package: atomic candidate namespace migration, full 12/5/3-file payloads and
synthetic artifact-only qualification. External release is the next separate gate.
No parallel agents or integration execution.

The row worker completed and v2 coordinator released Appendix A/§6 exclusively
before mutation. Fresh exact-name scan found no additional paths. Historical
standalone harnesses and receipts remain unchanged. Coordinator later allocated
one additional source path, `packages/source-provider-api/src/runtime-context.js`
inside the Skills repository, for the measured peer-admission correction below.
No handoff, coordination ledger, business prose or reference files were edited.

## Implemented result

- Both libraries and their bounded consumers now use `@flair-agency`; exact
  dependency pins and existing peer ranges are preserved. `lark-base-client`
  retains its old name. Provider/core/MCP/aggregate names and privacy flags remain.
- Three manifests have the contract's exact versions, exports, explicit file lists,
  MIT license and proposed Skills repository association. Publishable manifests
  omit `private`; this is **not** registry privacy evidence. No lifecycle hooks,
  bin, workflow, alias, automatic fallback or Provider dependency was added.
- The coin package directory contains only its manifest. The Skills workspace
  list explicitly selects the two runnable libraries plus fixtures; coin is built
  through the new assembler from authoritative existing Skill source.
- The assembler uses a frozen literal source-to-target map, an independently
  supplied inventory digest, exact ordinary-file snapshot closure, measured held
  bytes and a fresh destination. It rejects missing/extra files, changed digests,
  links, traversal inventory, reused/overlapping roots and map mutation. It never
  maintains a second editable Skill copy or invokes npm automatically.
- Original coin prose, agent YAML, references, shared helper and license bytes
  are included unchanged. All public business source deltas are exact library
  specifiers; the sole additional library behavior is described below.

### Measured portable peer-admission correction

The first new-name artifact test exposed an existing gap: portable loading accepted
an old-name-only API peer and returned exit 10. Legacy discovery checked its API
peer, but portable loading omitted that admission entirely. The accepted formal
contract required rejection; this was not treated as a passing rename.

Coordinator explicitly allocated the extra runtime-context path after examining
that failure. A fresh preimage was captured. Portable loading now requires
`peerDependencies[API_PACKAGE_NAME]` before descriptor/resource reads, returning
fixed safe `INCOMPATIBLE_API` on failure. It supports only exact numeric versions
and numeric caret ranges; unsupported syntax, missing/old-only peers and unsatisfied
ranges stop. Exact `1.6.0`, `^1.0.0`, `^1.4.0`, `^1.6.0` pass. This is a bounded
range implementation, not a claim to implement all npm semver syntax.
The descriptor's separate numeric API interval remains mandatory. No legacy
metadata prerequisite or legacy-discovery behavior was added; valid portable
fixtures also pass with `liveAgencyProvider` absent. API_VERSION is unchanged.

Initial failing evidence is retained in generations
`formal-artifacts-1788719276133-5571` and `formal-artifacts-1788719391673-6095`.
The second generation completed independent business/integrity checks before
reaching the still-unfixed peer case. Neither generation is accepted evidence.
Subsequent complete generations passed; only the final one below is definitive.

## Source locks and active development graph

Fixed Node **22.22.0**, npm **10.9.4**, macOS arm64; executable and npm tree hashes
are recorded in the final receipt. Source locks were generated by that npm with
`install --package-lock-only --ignore-scripts --offline --no-audit --no-fund`,
selected empty HOME/user/global config and generation-local caches. No dependency
was fetched and root postinstall did not run. All **108 existing integrity-bearing
root lock entries** remain unchanged as parsed objects; integrities were not
edited. Workspace metadata and only accepted dependency identities changed.

The first Skills lock-only run retained a historical extraneous
`test/fixtures/activity-pasted-source` entry with an old peer. A fresh, manifest-only
Skills workspace snapshot in designated fixtures generated the clean lock offline;
that measured npm output was applied to the Skills source lock. No hand-edited
lock removal and no active dependency reinstall was used. Original locks remain
in preimages. Artifact installation uses a separate lock containing only five local
reviewed tarballs, never workspace paths or remote resolutions.

Coordinator separately approved four exact reversible link replacements: the two
old library symlinks at root node_modules and the two in Skills node_modules were
replaced by new-name links to the same reviewed development package directories.
Original targets and scope metadata are in `links.json`; new-destination absence
and unchanged old links were checked first. Old aliases are absent; lark-base-client
and dependency payloads were not targeted. All **103 affected name references**
resolve to the same reviewed API/files source identities. No active `npm ci`,
dependency content deletion or operational-tree link was performed.

## Final artifact evidence

Receipt: [receipt.json](/Users/naokikimura/workspace/.live-agency-development/runs/formal-artifacts-1788719806126-7555/receipt.json).
Receipt SHA-256: `512c7b4dd6bcca269e8cc2850861ed755db143bf38c89347198e877ee4c1698f`.
It binds assembler/map/snapshot, qualification harness, toolchain, measured payloads,
commands, contexts, channel outputs, installed trees and preservation results.

| Artifact | Ordinary files | Tarball SHA-256 |
| --- | ---: | --- |
| `@flair-agency/source-provider-api@1.6.0` | 5 | `0b80c1b3dbd5f5d56aed2ba63f83915b1039cb9f3a63c0eb70bea76c8fdb4ca2` |
| `@flair-agency/private-runtime-files@1.0.0` | 3 | `4076996b2aa650f32c3327632f8f468bb6426830c6489ba2127d01bd18ce1c8b` |
| `@flair-agency/coin-expense-reconcile@1.0.0` | 12 | `57c26504eee67e35a0c5a95123ecaef4db2790ede28fe3636d1a8941dbb7a276` |
| `@fixture/coin-purchase-instruction-provider@1.0.0` | 3 | `e767a474afb87d1519fd17d240e7985c6dabc56304b0f442374d11d048caac92` |
| `@fixture/formal-coin-owner@1.0.0` | 1 | `56992cd827eef9354e47577e959c1c3b8c0616b17fa4fc123261b8e42b814c68` |

The first three are formal candidates; Provider and owner are separate private
synthetic test artifacts. The coin payload is 12 files, API 5, files library 3,
including exact LICENSE and package.json bytes. No private Provider, runtime
configuration, unrelated Skill, fixture or test joins the three formal artifacts.

Artifact lock SHA-256: `f5875d2fbed2cb3099528940aceeb23d9ae68205eefefec6d54ccbcb23461521`.
Both relocated `npm ci` runs began with absent node_modules and independent empty
caches, with offline/hooks-disabled configuration. Their complete ordinary-file
payload trees match (24 payload files plus npm's hidden lock). All installed file
hashes and hidden-lock entries match the inspected archives and artifact lock.
No extracted tree was repaired. Separate flat/nested layouts are deliberately
constructed from measured tar bytes; they are not claims about npm hoisting.

## Verification and exact limits

**1,010 distinct Node tests passed:** 980 affected-owner tests across 46 explicitly
selected files, plus 21 direct API/files/coin tests and 9 new payload tests. The
latest payload-only run supersedes its earlier result; reruns are not added.
No failures, skips or cancellations in the accepted logs. Commands and test file
selection are retained under [formal-distribution-20260907](/Users/naokikimura/workspace/.live-agency-development/runs/formal-distribution-20260907) in `affected-command.json`,
`affected.tap`, `source-direct-final.tap` and `payload-final.tap`.

The affected set includes coin weekly, fixture discovery, profile/activity/insight,
LIVE/invitation and their selected compaction callers, row receipt/storage/journal,
gift, maintenance/retention/drill pure callers, MCP runtime, relevant Provider
normalizers/session tests, synthetic root smoke and selected candidate tests.
It never runs service launchers or all-live conformance. After the narrow portable
peer correction, direct and new portable tests were rerun; unaffected legacy and
selected-caller results remain applicable. All 91 allocated JS/MJS files passed
syntax checks without executing launchers. Source public-content checker passed;
measured formal artifacts passed scoped content review and exact exclusion checks.
Regex success alone is not publication eligibility or history/license clearance.

The standalone final artifact harness separately verifies:

- 8 successful portable contexts (two natural npm installs, flat/nested layouts,
  four supported peer variants), each with absent and sentinel output: exit 10,
  exact instruction stdout and one metadata JSON line, output unchanged.
- 41 negative contexts, each in both output states: exit 2, no instruction text,
  one safe error record and untouched output. Scope, actor/profile, request/context
  tampering, unsupported modes, missing/ranged dependency, peer identity/range,
  descriptor changes, shadowing, no legacy fallback, traversal, oversized input,
  hardlink/symlink and file/directory modes are exercised. Missing descriptors in
  invalid-peer fixtures establish admission before descriptor/resource reads.
- Actual `ERR_ACCESS_DENIED` reads for source, snapshot, staging and the other
  install, while each selected route succeeds or rejects as expected.
- Full installed exports (`core`, `resolve`, `plan`, `prepare`, `verify`, SKILL.md)
  resolve and import with no CLI side effects. At a fixed synthetic clock, source
  and both installed coin packages produce identical plans, bundles, hashes,
  summaries and failures. Tests exercise blocked lookup, exact approval mismatch,
  stale replay and destination/linkage refusal with sentinel outputs retained.
- Explicit legacy expense and registration fixture instruction handoffs, without
  external registration or receipt acquisition.
- A still-valid gzip/tar with altered instruction bytes is refused by fresh-cache
  npm with `EINTEGRITY` against the unchanged lock. This does not protect a modified
  lock or prove registry reproducibility.

All IO is synthetic/local. Node permission tests do not certify full Codex host
isolation, deployed authority, live behavior or recoverability. Initial registry
privacy, organization rights and supported Actions authentication remain unverified
owner design choices. No registry/network/auth query, publish, workflow activation,
settings change, production operation, schedule or global installation occurred.

## Preservation, rollback and next package

[Preimages and final manifest](/tmp/formal-distribution-preimages-20260907) retain 117 path states, original worktree
bytes, per-file staged blob bytes, binary staged/unstaged diffs, raw indexes and
HEADs. Ten owner-directory snapshots resolve to **nine unique Git repositories**
(lark-core belongs to the root). Every original raw index, HEAD and staged diff
matches; unrelated tracked diffs match after excluding scoped deltas and resulting
submodule dirty markers. No Git stage or ref changed.

The 24 accepted row-worker source hashes were verified: 22 are identical and the
two compaction callers differ only by the approved library specifier. Their accepted
logic remains intact. Journal markers, old archives and recovery generations were
not targeted; new synthetic generations are retained. The package did not capture
a pre-change full node_modules byte inventory, so preservation evidence is scoped
to exact link operations, source/index checks and npm's lock-only operations.

`final-manifest.json` contains every changed path and before/after SHA-256, including
this report. `preservation-review.json` records row/index/diff/link checks. The
selective `rollback.py --check` validates current hashes and link targets without
mutation. Its separately invoked `--apply` restores only package preimages/removes
only package-created files after freshness checks, reverses the four link deltas,
and leaves indexes, unrelated dirty work, journal markers and synthetic archives
untouched. Later changed files/links stop rollback rather than being overwritten.

**One next package:** coordinator acceptance and a narrowly authorized registry/
Actions capability and release-source readiness check, before any publish. Freeze
reviewed source revisions and separately verify organization/package ownership,
initial private visibility, supported actor permissions and digest-bound manual
approval. No release workflow is active. Host Skill registration/SEP-2 authority
and operational cutover remain separate, unperformed gates.
