# CI-suppressed npm runtime source checkpoint

2026-09-07 JST. **Complete: one independent local source commit and verified linkage to the existing artifacts.** No upload, publication, reconstruction, install or repository code execution occurred.

Primary class G: deterministic source checkpoint. Protected boundary: initial-push workflow discovery. Invariants: retain accepted sources, history, refs/indexes, artifacts and reports; preserve all package inputs. One package: apply the accepted exact rename patch in a fresh independent local repository, create one commit, and verify source-to-existing-artifact lineage. No parallel work, subagent, successor, cross-task message or model override occurred; configured settings were retained.

Read basis: applicable AGENTS, task orchestration policy, complete authoritative Private Source Integration Guide, initial-push suppression report, release-source checkpoint and lineage, and source publication eligibility report. No Skill contract was changed or requalified.

## Checkpoint and authority

The coordinator accepted patch SHA-256 `23dd0b138d0ed92b37ff3941d8b4da27090362bc764b1d7be78a89be69172aa9` and authorized this new source checkpoint. Normal scoped filesystem review permitted creation of only the designated new generation. This authority supplies no publication or private-history disclosure permission.

| Binding | Actual value |
| --- | --- |
| Independent repository | `/Users/naokikimura/workspace/.live-agency-development/runs/ci-suppressed-source-commit-20260907-01/skills` |
| Branch | `codex/npm-ci-suppressed-source` |
| Exact parent | `f2a476a6e22b1510e262e79cda651c764aa68953` |
| Parent tree | `0117c308c92851e61256ef97399e2f3a94eb8fca` |
| New commit | `c90c554b2880ffe3f4c21ee621745c42f81ca287` |
| New tree | `1e7eb0452aca676e5e01fa500afb279b73702f5f` |
| Verification receipt SHA-256 | `748b7304734459e804762c6bb0ad315caeff298db75a23410280f1e4124adf48` |
| New lineage receipt SHA-256 | `58abd2312e554993eb91ce50a35a7027ff0d877dee7cfa47c9423408929de03e` |

The generation is owner-only (0700), created under umask 0077. Local clone used `--local --no-hardlinks --no-checkout --no-recurse-submodules --template=`; origin was removed before checkout. No remotes or alternates remain; the Git directory is independent and object-file link counts are one. Hooks, signing, global/system configuration, automatic maintenance, submodule recursion, fsmonitor, replacement objects and optional locks were disabled. The explicitly selected author and committer match the accepted parent identity, extracted only from that commit, with no ambient identity or credential lookup. Identity values remain in local commit objects, not this report.

## Focused verification

The exact patch passed `git apply --index --check`. Before committing, the full resulting path/mode/type/blob map matched the parent map with only `.github/workflows/ci.yml` moved to `docs/ci-reference/ci.yml.disabled`. Both paths bind blob `7c130e15bb34ecc8542cbbdd11cce1ce95e33630`, ordinary mode `100644`; all other 181 entries are identical, with 182 total. The resulting tree matches the previously computed expected tree. No tip entry remains under `.github/workflows/`.

The new commit has exactly the specified parent and is the only additional commit. All 34 inherited commits remain reachable (35 total); parent commit bytes match the accepted repository. The new checkout is clean, and `git fsck --full --no-reflogs` passed. The retained independent repository is the recovery artifact; no additional bundle, broad backup or repacking was performed.

Before/after receipts compare HEAD, refs, index SHA-256, and separate staged/unstaged binary-diff digests for the active root, active Skills checkout and accepted checkpoint. All match. Their HEADs remain respectively `6b1f11c17cec53f2adcce5fd88b87666c322f859`, `e78ec0962184fee2b07ea85b3f24545438f232d4`, and the exact parent above. Accepted sources and existing evidence were opened only for reading. This report is the only authored file in the active checkout; its index was not staged or changed.

Retained evidence under the new generation: `before.json`, `after.json`, `commit-diff.txt`, `commit-verification.json`, and `payload-lineage.json`. The lineage binds the new commit/tree to the unchanged 20 mappings, 18 unique source inputs, supporting assembler/map blobs and the three existing archives. Its predecessor remains SHA-256 `876ba98fe00d52c52568e43c33ef263cd09f77af2ad7b3e636ce47413dd5032c`; the original artifact receipt remains `512c7b4dd6bcca269e8cc2850861ed755db143bf38c89347198e877ee4c1698f`.

## Existing artifact linkage

Every mapped committed blob ID and source SHA-256 matched the accepted lineage. Existing tarballs were read without extraction to disk; their ordinary payload files match committed bytes exactly, with no additional file or special-entry payload. All three archive SHA-256 and SHA-512 integrity values match the accepted receipt. These are inherited, unmodified artifacts checked against new source identity, not newly reconstructed artifacts.

| Existing formal artifact | Files | SHA-256 |
| --- | ---: | --- |
| `@flair-agency/source-provider-api@1.6.0` | 5 | `0b80c1b3dbd5f5d56aed2ba63f83915b1039cb9f3a63c0eb70bea76c8fdb4ca2` |
| `@flair-agency/private-runtime-files@1.0.0` | 3 | `4076996b2aa650f32c3327632f8f468bb6426830c6489ba2127d01bd18ce1c8b` |
| `@flair-agency/coin-expense-reconcile@1.0.0` | 12 | `57c26504eee67e35a0c5a95123ecaef4db2790ede28fe3636d1a8941dbb7a276` |

Assembler blob remains `8c79777614cfb53d0a579d24b11b820a9eafb218`, SHA-256 `4de73665fb8f68cec9e6d1a7721e62cddd6bb435cd144cc649396339fc24ca4c`. Map blob remains `982eb3e63b3496d7047c5e45dc24b6e990202058`, SHA-256 `3073947181fdf710425e9a5b57e72cef74608859bd96325f0b99cd7a28f02286`. The two fixture archives remain excluded qualification evidence. Prior test results are inherited; no tests or clean-commit packing ran here.

## Limits and next gate

The reconstruction operation remains **rejected before execution by automatic review despite direct owner approval**, as recorded in the task scope. Its script was neither invoked nor changed, and no alternative reconstruction route was attempted. This separately authorized source checkpoint does not clear that gate.

The historical workflow remains in ancestor commits; this tip rename does not establish blanket destination Actions suppression. Destination settings, default branch, other refs, workflows and event behavior remain unverified. Owner copyright/provenance, holder-name transition and E1/E2/E4 private-history disclosure responses remain pending and must bind the new checkpoint plus its inherited history. SEP-2 host/authority isolation, scoped remote actor/resource selection, privacy/access/version freshness, private qualification-source closure, qualified workflow/runner and digest-bound publication approval remain open. No network, authentication, push, workflow activation, global registration or schedule action occurred.

Recommended next package: coordinator acceptance of this exact checkpoint and lineage, followed by the pending owner provenance/private-history disposition. Do not automatically resume reconstruction or remote work. Rollback is abandonment of only this new generation and report after checking for later owner changes; never reset accepted or active sources, refs, indexes, artifacts or prior reports.
